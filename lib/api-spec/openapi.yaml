openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: A3 Partner Portal API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: partners
    description: Partner management
  - name: requests
    description: Request management
  - name: assets
    description: Asset management
  - name: pricing
    description: Pricing rules
  - name: public
    description: Public partner portal
  - name: dashboard
    description: Admin dashboard
  - name: Storage
    description: Object storage upload and serving endpoints.
  - name: production
    description: Production handoff and supplier packets.
  - name: admin
    description: Internal admin endpoints (response-shape guards only).
  - name: units
    description: Measurement-unit metadata and resolution.

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /partners:
    get:
      operationId: listPartners
      tags: [partners]
      summary: List all partners
      parameters:
        - name: active
          in: query
          schema:
            type: boolean
      responses:
        "200":
          description: List of partners
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Partner"
    post:
      operationId: createPartner
      tags: [partners]
      summary: Create a new partner
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePartnerBody"
      responses:
        "201":
          description: Created partner
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Partner"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /partners/{id}:
    get:
      operationId: getPartner
      tags: [partners]
      summary: Get partner by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Partner details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Partner"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
    patch:
      operationId: updatePartner
      tags: [partners]
      summary: Update a partner
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePartnerBody"
      responses:
        "200":
          description: Updated partner
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Partner"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
    delete:
      operationId: deletePartner
      tags: [partners]
      summary: Delete a partner
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /partners/{id}/assets:
    get:
      operationId: listPartnerAssets
      tags: [assets]
      summary: List assets for a partner
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of partner assets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PartnerAsset"
    post:
      operationId: createPartnerAsset
      tags: [assets]
      summary: Add asset to partner
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePartnerAssetBody"
      responses:
        "201":
          description: Created asset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartnerAsset"

  /partners/{id}/assets/{assetId}:
    delete:
      operationId: deletePartnerAsset
      tags: [assets]
      summary: Delete a partner asset
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: assetId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /public/partners/{slug}:
    get:
      operationId: getPublicPartner
      tags: [public]
      summary: Get public partner page data
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Public partner data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicPartner"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /public/partners/{slug}/requests:
    post:
      operationId: submitPublicRequest
      tags: [public]
      summary: Submit a public request through partner portal
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitRequestBody"
      responses:
        "201":
          description: Request submitted successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubmitRequestResponse"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          description: Partner not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /public/pricing:
    get:
      operationId: getPublicPricing
      tags: [public]
      summary: Get public starting-at pricing
      responses:
        "200":
          description: Public pricing data
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicPricingItem"

  /requests:
    get:
      operationId: listRequests
      tags: [requests]
      summary: List all requests with filters
      parameters:
        - name: partnerId
          in: query
          schema:
            type: integer
        - name: status
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: List of requests
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestListResponse"

  /requests/{id}:
    get:
      operationId: getRequest
      tags: [requests]
      summary: Get request detail
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Full request detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
    patch:
      operationId: updateRequest
      tags: [requests]
      summary: Update request status or fields
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRequestBody"
      responses:
        "200":
          description: Updated request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /requests/{id}/notes:
    get:
      operationId: listRequestNotes
      tags: [requests]
      summary: List notes for a request
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of notes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminNote"
    post:
      operationId: createRequestNote
      tags: [requests]
      summary: Add a note to a request
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateNoteBody"
      responses:
        "201":
          description: Created note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminNote"

  /requests/{id}/regenerate-ai:
    post:
      operationId: regenerateAiSummary
      tags: [requests]
      summary: Regenerate AI summary for a request
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Regenerated summaries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestDetail"

  /requests/{id}/regenerate-pdf:
    post:
      operationId: regeneratePdf
      tags: [requests]
      summary: Regenerate PDF summary for a request
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: PDF URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PdfResponse"

  /pricing-rules:
    get:
      operationId: listPricingRules
      tags: [pricing]
      summary: List all pricing rules
      parameters:
        - name: category
          in: query
          schema:
            type: string
        - name: active
          in: query
          schema:
            type: boolean
      responses:
        "200":
          description: List of pricing rules
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PricingRule"
    post:
      operationId: createPricingRule
      tags: [pricing]
      summary: Create a pricing rule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePricingRuleBody"
      responses:
        "201":
          description: Created pricing rule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PricingRule"

  /pricing-rules/{id}:
    patch:
      operationId: updatePricingRule
      tags: [pricing]
      summary: Update a pricing rule
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePricingRuleBody"
      responses:
        "200":
          description: Updated pricing rule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PricingRule"
    delete:
      operationId: deletePricingRule
      tags: [pricing]
      summary: Delete a pricing rule
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /dashboard/summary:
    get:
      operationId: getDashboardSummary
      tags: [dashboard]
      summary: Get admin dashboard summary
      responses:
        "200":
          description: Dashboard summary data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardSummary"

  /dashboard/recent-requests:
    get:
      operationId: getRecentRequests
      tags: [dashboard]
      summary: Get recent requests for dashboard
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
      responses:
        "200":
          description: Recent requests
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RequestSummary"

  /dashboard/requests-by-status:
    get:
      operationId: getRequestsByStatus
      tags: [dashboard]
      summary: Get request counts grouped by status
      responses:
        "200":
          description: Status breakdown
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StatusCount"

  /assets/library:
    get:
      operationId: getAssetsLibrary
      tags: [assets]
      summary: Get all assets grouped by partner and request
      responses:
        "200":
          description: Assets library
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetsLibrary"

  /orders/{orderId}/supplier-packet/{supplierId}:
    get:
      operationId: getSupplierPacket
      tags: [production]
      summary: Get the production packet for a supplier on an order
      description: |
        Returns the supplier-facing production packet for a single order/supplier
        pair: order header + logistics, partner/event/supplier identity, and the
        line items assigned to this supplier with their resolved specs, pricing
        basis, asset links (current + approved + vendor-visible only), and
        readiness flags.
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: integer
        - name: supplierId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Supplier production packet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupplierPacketResponse"
        "404":
          description: Order or supplier not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /orders/{id}/readiness:
    get:
      operationId: getOrderReadiness
      tags: [production]
      summary: Get production readiness for an order
      description: |
        Returns per-line-item production readiness info (expectations, asset
        links, blocked reasons, flags) plus a roll-up summary for the order.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Order readiness
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrderReadinessResponse"
        "400":
          description: Invalid id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /order-items/{id}/production-block:
    patch:
      operationId: setOrderItemProductionBlock
      tags: [production]
      summary: Set or clear the production-blocked reason on a line item
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
              properties:
                reason: { type: ["string", "null"] }
                overrideNote: { type: ["string", "null"] }
      responses:
        "200":
          description: Updated order item row
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrderItemProductionBlockResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /production/dashboard:
    get:
      operationId: getProductionDashboard
      tags: [production]
      summary: Production review dashboard counters and recent activity
      responses:
        "200":
          description: Production dashboard payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductionDashboardResponse"

  /storage/uploads/request-url:
    post:
      tags: [Storage]
      operationId: requestUploadUrl
      summary: Request a presigned URL for file upload
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UploadUrlRequest'
      responses:
        '200':
          description: Presigned upload URL generated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadUrlResponse'
        '400':
          description: Missing or invalid required fields.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Failed to generate upload URL.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'

  /storage/public-objects/{filePath}:
    get:
      tags: [Storage]
      operationId: getPublicObject
      summary: Serve a public asset
      parameters:
        - name: filePath
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Object content streamed.
          content:
            '*/*':
              schema:
                type: string
                format: binary
        '404':
          description: File not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'

  /storage/objects/{objectPath}:
    get:
      tags: [Storage]
      operationId: getStorageObject
      summary: Serve an object entity
      parameters:
        - name: objectPath
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Object content streamed.
          content:
            '*/*':
              schema:
                type: string
                format: binary
        '404':
          description: Object not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'

  /public/integrations/asset-survey/{partnerSlug}:
    post:
      operationId: surveyWebhook
      tags: [public]
      summary: Inbound webhook from the Venue Asset Survey app
      description: HMAC-SHA256 signed (header `X-Survey-Signature`) ingest of one or more SurveyAsset payloads. Per-partner secret.
      parameters:
        - name: partnerSlug
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                assets:
                  type: array
                  items: { $ref: "#/components/schemas/SurveyAssetIngest" }
              required: [assets]
      responses:
        "200":
          description: Ingest result
          content:
            application/json:
              schema:
                type: object
                required: [created, updated]
                properties:
                  created: { type: integer }
                  updated: { type: integer }
        "401": { description: Invalid signature, content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } } }
        "404": { description: Unknown partner slug, content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } } }

  /admin/integrations/asset-survey/pull/{partnerId}:
    post:
      operationId: surveyAdminPull
      tags: [public]
      summary: Admin-triggered pull from the survey app for a partner
      parameters:
        - name: partnerId
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Pull result
          content:
            application/json:
              schema:
                type: object
                required: [created, updated]
                properties:
                  created: { type: integer }
                  updated: { type: integer }

  /admin/integrations/asset-survey/test/{partnerId}:
    post:
      operationId: surveyTestConnection
      tags: [public]
      summary: Probe the configured survey app to confirm it is reachable
      parameters:
        - name: partnerId
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Probe result (`ok=false` is still 200 for diagnostics)
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
                  status: { type: integer }
                  probedUrl: { type: string }
                  apiKeyPresent: { type: boolean }
                  message: { type: string }
                  error: { type: string }

  /admin/survey-assets:
    get:
      operationId: surveyAssetsList
      tags: [public]
      summary: List all survey assets (filterable by status / partner)
      parameters:
        - name: status
          in: query
          schema: { type: string, enum: [pending, approved, rejected, all] }
        - name: partnerId
          in: query
          schema: { type: integer }
      responses:
        "200":
          description: Asset list
          content:
            application/json:
              schema:
                type: object
                required: [assets]
                properties:
                  assets:
                    type: array
                    items: { $ref: "#/components/schemas/SurveyAsset" }

  /admin/survey-assets/{id}:
    patch:
      operationId: surveyAssetPatch
      tags: [public]
      summary: Approve, reject, or edit a survey asset
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SurveyAssetPatch" }
      responses:
        "200":
          description: Updated asset
          content:
            application/json:
              schema:
                type: object
                required: [asset]
                properties:
                  asset: { $ref: "#/components/schemas/SurveyAsset" }
    delete:
      operationId: surveyAssetDelete
      tags: [public]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200": { description: Deleted, content: { application/json: { schema: { type: object, required: [ok], properties: { ok: { type: boolean } } } } } }

  /public/partners/{slug}/survey-assets:
    get:
      operationId: surveyPublicList
      tags: [public]
      summary: Public approved survey assets for the partner portal "Brand our space" tile gallery
      parameters:
        - name: slug
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Public-projected assets only (no measurements, no internal fields)
          content:
            application/json:
              schema:
                type: object
                required: [assets]
                properties:
                  assets:
                    type: array
                    items: { $ref: "#/components/schemas/PublicSurveyAsset" }

  # ── Permissive admin endpoints (response-shape guards only) ───────────
  # The handlers in this section already validate inputs strictly. The
  # response schemas here exist so that the generated *Response zod schemas
  # let us safeParse the outgoing payload and surface a logged 500 if a
  # handler accidentally returns the wrong shape (string/null/array vs the
  # expected envelope). Schemas are intentionally permissive
  # (additionalProperties: true, no required fields) so they only catch
  # outright shape regressions, not field-level drift.

  /public-config:
    get:
      operationId: getPublicConfig
      tags: [public]
      responses:
        "200": { description: Public config, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /units/meta:
    get:
      operationId: getUnitsMeta
      tags: [units]
      responses:
        "200": { description: Units metadata, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /units/resolve:
    get:
      operationId: getUnitsResolve
      tags: [units]
      responses:
        "200": { description: Resolved unit preference, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /security/readiness:
    get:
      operationId: getSecurityReadiness
      tags: [admin]
      responses:
        "200": { description: Security readiness report, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /deployment/readiness:
    get:
      operationId: getDeploymentReadiness
      tags: [admin]
      responses:
        "200": { description: Deployment readiness report, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /cities:
    get:
      operationId: listCities
      tags: [admin]
      responses:
        "200": { description: List of cities, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
    post:
      operationId: createCity
      tags: [admin]
      responses:
        "201": { description: Created city, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /cities/{id}:
    patch:
      operationId: updateCity
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Updated city, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deleteCity
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Delete acknowledgement, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /suppliers:
    get:
      operationId: listSuppliers
      tags: [admin]
      responses:
        "200": { description: List of suppliers, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
    post:
      operationId: createSupplier
      tags: [admin]
      responses:
        "201": { description: Created supplier, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /suppliers/{id}:
    get:
      operationId: getSupplier
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Supplier, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    patch:
      operationId: updateSupplier
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Updated supplier, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deleteSupplier
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Delete acknowledgement, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /user-roles:
    get:
      operationId: listUserRoles
      tags: [admin]
      responses:
        "200": { description: List of user roles, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
    post:
      operationId: createUserRole
      tags: [admin]
      responses:
        "201": { description: Created role, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /user-roles/{id}:
    patch:
      operationId: updateUserRole
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Updated role, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deleteUserRole
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Delete acknowledgement, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /saved-addresses:
    get:
      operationId: listSavedAddresses
      tags: [admin]
      responses:
        "200": { description: Addresses, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
    post:
      operationId: createSavedAddress
      tags: [admin]
      responses:
        "201": { description: Created, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /saved-addresses/{id}:
    patch:
      operationId: updateSavedAddress
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Updated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deleteSavedAddress
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Delete acknowledgement, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /saved-contacts:
    get:
      operationId: listSavedContacts
      tags: [admin]
      responses:
        "200": { description: Contacts, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
    post:
      operationId: createSavedContact
      tags: [admin]
      responses:
        "201": { description: Created, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /saved-contacts/{id}:
    patch:
      operationId: updateSavedContact
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Updated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deleteSavedContact
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Delete acknowledgement, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /partners/{id}/theme:
    get:
      operationId: getPartnerTheme
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Theme or null, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObjectOrNull" } } } }
    put:
      operationId: upsertPartnerTheme
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Updated theme, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
        "201": { description: Created theme, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /partners/{id}/inventory-summary:
    get:
      operationId: getPartnerInventorySummary
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Partner inventory summary, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /partners/{id}/sections:
    get:
      operationId: listPartnerSections
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Sections, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
    post:
      operationId: createPartnerSection
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "201": { description: Created section, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /partners/{id}/sections/{sectionId}:
    patch:
      operationId: updatePartnerSection
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
        - { name: sectionId, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Updated section, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /partners/{id}/sections/bulk:
    put:
      operationId: bulkReplacePartnerSections
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Replaced sections, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }

  /partners/{id}/branding-locations:
    get:
      operationId: listBrandingLocations
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Locations, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
    post:
      operationId: createBrandingLocation
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "201": { description: Created location, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /partners/{id}/branding-locations/bulk:
    post:
      operationId: bulkCreateBrandingLocations
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "201": { description: Created locations, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }

  /partners/{id}/branding-locations/{locationId}:
    patch:
      operationId: updateBrandingLocation
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
        - { name: locationId, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Updated location, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /partners/{id}/branding-locations/bulk-update:
    post:
      operationId: bulkUpdateBrandingLocations
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Updated locations, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }

  /partners/{id}/email-recipients:
    get:
      operationId: listEmailRecipients
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Recipients, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
    post:
      operationId: createEmailRecipient
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "201": { description: Created recipient, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /partners/{id}/email-recipients/{rid}:
    put:
      operationId: updateEmailRecipient
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
        - { name: rid, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Updated recipient, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deleteEmailRecipient
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
        - { name: rid, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Delete acknowledgement, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /partners/{id}/test-role-email:
    post:
      operationId: testRoleEmail
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Email send result, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
        "500": { description: Email send result (failure), content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /onboarding/progress:
    get:
      operationId: getOnboardingProgress
      tags: [public]
      responses:
        "200": { description: Onboarding progress rows, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
    post:
      operationId: upsertOnboardingProgress
      tags: [public]
      responses:
        "200": { description: Upserted progress row, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /onboarding/dismiss:
    post:
      operationId: dismissOnboarding
      tags: [public]
      responses:
        "200": { description: Dismiss acknowledgement, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /launch/platform:
    get:
      operationId: getLaunchPlatform
      tags: [admin]
      responses:
        "200": { description: Platform launch readiness, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /launch/partner/{id}:
    get:
      operationId: getLaunchPartner
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Partner launch readiness, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /launch/partner/{id}/activate:
    post:
      operationId: activateLaunch
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: New launch status, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /public/partnership-requests:
    post:
      operationId: submitPartnershipRequest
      tags: [public]
      responses:
        "201": { description: Submitted, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /events:
    get:
      operationId: listEvents
      tags: [admin]
      responses:
        "200": { description: Events, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
  /events/{id}:
    get:
      operationId: getEvent
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Event, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    patch:
      operationId: updateEvent
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Updated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deleteEvent
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Deleted, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /partners/{id}/addons:
    get:
      operationId: listPartnerAddons
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Addons, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
    put:
      operationId: replacePartnerAddons
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Replaced, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
  /events/{id}/addons/effective:
    get:
      operationId: getEventEffectiveAddons
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Effective addons, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /public/partners/{slug}/events/{eventId}/addons:
    get:
      operationId: getPublicEventAddons
      tags: [public]
      parameters:
        - { name: slug, in: path, required: true, schema: { type: string } }
        - { name: eventId, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Public addons, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /analytics/kpis:
    get: { operationId: getAnalyticsKpis, tags: [admin], responses: { "200": { description: KPIs, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } } } }
  /analytics/profitability:
    get: { operationId: getAnalyticsProfitability, tags: [admin], responses: { "200": { description: Profitability, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }
  /analytics/suppliers:
    get: { operationId: getAnalyticsSuppliers, tags: [admin], responses: { "200": { description: Supplier perf, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }
  /analytics/packages:
    get: { operationId: getAnalyticsPackages, tags: [admin], responses: { "200": { description: Packages, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }
  /analytics/zones:
    get: { operationId: getAnalyticsZones, tags: [admin], responses: { "200": { description: Zones, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }
  /analytics/products:
    get: { operationId: getAnalyticsProducts, tags: [admin], responses: { "200": { description: Products, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }
  /analytics/forecast:
    get: { operationId: getAnalyticsForecast, tags: [admin], responses: { "200": { description: Forecast, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } } } }
  /analytics/risk:
    get: { operationId: getAnalyticsRisk, tags: [admin], responses: { "200": { description: Risk, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } } } }
  /analytics/trends:
    get: { operationId: getAnalyticsTrends, tags: [admin], responses: { "200": { description: Trends, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }

  /imports/fields/{resource}:
    get:
      operationId: getImportFields
      tags: [admin]
      parameters: [{ name: resource, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Fields, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /imports/parse:
    post:
      operationId: parseImport
      tags: [admin]
      responses:
        "200": { description: Parse result, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /imports/commit:
    post:
      operationId: commitImport
      tags: [admin]
      responses:
        "200": { description: Commit result, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /billing/orders/{id}/resolve:
    get:
      operationId: resolveBilling
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Resolved, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /billing/orders/{id}/override:
    post:
      operationId: overrideBilling
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Updated order, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /billing/summary:
    get:
      operationId: getBillingSummary
      tags: [admin]
      responses:
        "200": { description: Summary, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /billing/orders:
    get:
      operationId: listBillingOrders
      tags: [admin]
      responses:
        "200": { description: Billing orders, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
  /billing/bulk:
    post:
      operationId: billingBulk
      tags: [admin]
      responses:
        "200": { description: Bulk result, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /admin/live-readiness:
    get:
      operationId: getLiveReadiness
      tags: [admin]
      responses:
        "200": { description: Readiness probe, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /packages:
    get: { operationId: listPackages, tags: [admin], responses: { "200": { description: Packages, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }
  /packages/{id}:
    get:
      operationId: getPackage
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Package, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    patch:
      operationId: updatePackage
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Updated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deletePackage
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Deleted, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /packages/reorder:
    post: { operationId: reorderPackages, tags: [admin], responses: { "200": { description: Reordered, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } } } }
  /package-items/{itemId}:
    patch:
      operationId: updatePackageItem
      tags: [admin]
      parameters: [{ name: itemId, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Updated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deletePackageItem
      tags: [admin]
      parameters: [{ name: itemId, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Deleted, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /partners/{partnerId}/contacts:
    get:
      operationId: listPartnerContacts
      tags: [admin]
      parameters: [{ name: partnerId, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Contacts, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } }
  /partner-contacts/{id}:
    patch:
      operationId: updatePartnerContact
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Updated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deletePartnerContact
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Deleted, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /partner-contacts/{id}/make-primary:
    post:
      operationId: makePrimaryPartnerContact
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Primary, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /onboarding/submissions:
    get: { operationId: listOnboardingSubmissions, tags: [admin], responses: { "200": { description: Submissions, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }
  /onboarding/submissions/{id}:
    get:
      operationId: getOnboardingSubmission
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Submission, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    patch:
      operationId: updateOnboardingSubmission
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Updated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /admin/alerts:
    get: { operationId: listAdminAlerts, tags: [admin], responses: { "200": { description: Alerts, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } } } }
  /admin/alerts/summary:
    get: { operationId: getAdminAlertsSummary, tags: [admin], responses: { "200": { description: Summary, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } } } }
  /admin/alerts/partner/{partnerId}:
    get:
      operationId: getPartnerAlerts
      tags: [admin]
      parameters: [{ name: partnerId, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Alerts, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /admin/alerts/order/{orderId}:
    get:
      operationId: getOrderAlerts
      tags: [admin]
      parameters: [{ name: orderId, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Alerts, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /admin/alerts/manual-followup/{id}/resolve:
    post:
      operationId: resolveManualFollowup
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Resolved, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /admin/support-issues/{issueId}/resolve:
    post:
      operationId: resolveSupportIssue
      tags: [admin]
      parameters: [{ name: issueId, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Resolved, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /admin/partners/{id}/archive:
    post:
      operationId: archivePartner
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Archived, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /admin/partners/{id}/unarchive:
    post:
      operationId: unarchivePartner
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Unarchived, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /admin/assets/{id}/archive:
    post:
      operationId: archiveAsset
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Archived, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /admin/assets/{id}/unarchive:
    post:
      operationId: unarchiveAsset
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Unarchived, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /invoices:
    get: { operationId: listInvoices, tags: [admin], responses: { "200": { description: Invoices, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }
  /invoices/{id}:
    get:
      operationId: getInvoice
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Invoice, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    patch:
      operationId: updateInvoice
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Updated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /invoices/public/{token}:
    get:
      operationId: getPublicInvoice
      tags: [public]
      parameters: [{ name: token, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: Invoice, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /invoices/{id}/regenerate:
    post:
      operationId: regenerateInvoice
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Regenerated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /invoices/{id}/payments/{pid}:
    delete:
      operationId: deleteInvoicePayment
      tags: [admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer } }
        - { name: pid, in: path, required: true, schema: { type: integer } }
      responses:
        "200": { description: Deleted, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /invoices/scan-overdue:
    post: { operationId: scanOverdueInvoices, tags: [admin], responses: { "200": { description: Scanned, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } } } }

  /inventory:
    get: { operationId: listInventory, tags: [admin], responses: { "200": { description: Inventory, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }
  /inventory/shortages:
    get: { operationId: getInventoryShortages, tags: [admin], responses: { "200": { description: Shortages, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }
  /inventory/{id}:
    patch:
      operationId: updateInventory
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Updated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deleteInventory
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Deleted, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /inventory/reservations:
    get: { operationId: listInventoryReservations, tags: [admin], responses: { "200": { description: Reservations, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }
  /inventory/reservations/{id}:
    patch:
      operationId: updateInventoryReservation
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Updated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deleteInventoryReservation
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Deleted, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }

  /assets:
    get: { operationId: listAssets, tags: [admin], responses: { "200": { description: Assets, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }
  /assets/{id}:
    get:
      operationId: getAsset
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Asset, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    patch:
      operationId: updateAsset
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Updated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
    delete:
      operationId: deleteAsset
      tags: [admin]
      parameters: [{ name: id, in: path, required: true, schema: { type: integer } }]
      responses:
        "200": { description: Deleted, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericObject" } } } }
  /asset-events:
    get: { operationId: listAssetEvents, tags: [admin], responses: { "200": { description: Asset events, content: { application/json: { schema: { $ref: "#/components/schemas/AdminGenericArray" } } } } } }

components:
  schemas:
    # Permissive passthrough envelopes used by the response-shape-only
    # admin endpoints. They only assert the top-level JSON type
    # (object / array / nullable object) and are NOT meant to lock down
    # field-level shape — the handlers themselves still own that.
    AdminGenericObject:
      type: object
      additionalProperties: true
    AdminGenericObjectOrNull:
      oneOf:
        - { type: object, additionalProperties: true }
        - { type: "null" }
    AdminGenericArray:
      type: array
      items:
        type: object
        additionalProperties: true

    SurveyAssetPhoto:
      type: object
      required: [url]
      properties:
        url: { type: string }
        caption: { type: string }

    SurveyAssetMeasurements:
      type: object
      description: Physical measurements. A3-internal — never exposed on /public/* projections.
      additionalProperties: false
      properties:
        widthIn: { type: ["number", "null"] }
        heightIn: { type: ["number", "null"] }
        depthIn: { type: ["number", "null"] }
        diameterIn: { type: ["number", "null"] }
        areaSqft: { type: ["number", "null"] }
        shape: { type: ["string", "null"] }
        measurementUnit:
          type: ["string", "null"]
          enum: [in, cm, ft, null]
          description: Unit the survey app captured. Intake email/UI render measurements in this unit.
        orientation:
          type: ["string", "null"]
          description: landscape | portrait | square | free

    SurveyAssetSurface:
      type: object
      additionalProperties: false
      properties:
        surfaceMaterial: { type: ["string", "null"], description: Physical mounting surface (drywall, glass, fabric, brick…) }
        environment: { type: ["string", "null"], description: indoor | outdoor | covered_outdoor }
        zoneName: { type: ["string", "null"], description: Venue zone (Lobby, Concourse, Suite Level…) }

    SurveyAssetApplications:
      type: object
      additionalProperties: false
      properties:
        primary:
          type: ["array", "null"]
          items: { type: string }
        recommended:
          type: ["array", "null"]
          items: { type: string }
        alternate:
          type: ["array", "null"]
          items: { type: string }
        publicUseCase: { type: ["string", "null"] }

    SurveyAssetVisibility:
      type: object
      additionalProperties: false
      properties:
        visibilityTier:
          type: ["string", "null"]
          description: hero | featured | standard | hidden
        publicStatus:
          type: ["string", "null"]
          description: live | draft | retired
        publicDeckInclude: { type: ["boolean", "null"] }
        portalVisible: { type: ["boolean", "null"] }
        netsuiteInclude: { type: ["boolean", "null"] }
        designNeeded: { type: ["boolean", "null"] }
        commissionEligible: { type: ["boolean", "null"] }
        opsOwner: { type: ["string", "null"] }

    SurveyAssetInternal:
      type: object
      description: Internal-only fields the survey app attaches. Stored server-side, never echoed on /public/* projections.
      additionalProperties: false
      properties:
        notes: { type: ["string", "null"] }
        installNotes: { type: ["string", "null"] }
        productionNotes: { type: ["string", "null"] }
        pricingNotes: { type: ["string", "null"] }
        photos:
          type: ["array", "null"]
          items: { $ref: "#/components/schemas/SurveyAssetPhoto" }
        netsuiteAssetNumber: { type: ["string", "null"] }
        netsuiteVenueNumber: { type: ["string", "null"] }
        netsuiteItemName: { type: ["string", "null"] }
        netsuiteItemCategory: { type: ["string", "null"] }
        costCenter: { type: ["string", "null"] }
        surveyorName: { type: ["string", "null"] }
        surveyedAt: { type: ["string", "null"], description: "ISO 8601 date-time" }

    SurveyAssetIngest:
      type: object
      description: Payload shape sent by the Venue Asset Survey app (subset of Asset_Master columns). Measurements and internal/ops-only fields are nested so /public/* projections can guarantee they never leak.
      additionalProperties: false
      required: [externalAssetId, name]
      properties:
        externalAssetId: { type: string }
        externalSurveyId: { type: ["string", "null"] }
        name: { type: string }
        description: { type: ["string", "null"] }
        category: { type: ["string", "null"] }
        venueName: { type: ["string", "null"] }
        cityName: { type: ["string", "null"] }
        publicPhotoUrl: { type: ["string", "null"] }
        publicPhotos:
          type: ["array", "null"]
          items: { $ref: "#/components/schemas/SurveyAssetPhoto" }
        measurements:
          oneOf:
            - { $ref: "#/components/schemas/SurveyAssetMeasurements" }
            - { type: "null" }
        surface:
          oneOf:
            - { $ref: "#/components/schemas/SurveyAssetSurface" }
            - { type: "null" }
        applications:
          oneOf:
            - { $ref: "#/components/schemas/SurveyAssetApplications" }
            - { type: "null" }
        visibility:
          oneOf:
            - { $ref: "#/components/schemas/SurveyAssetVisibility" }
            - { type: "null" }
        approvedMaterials:
          type: ["array", "null"]
          items: { type: string }
        internal:
          oneOf:
            - { $ref: "#/components/schemas/SurveyAssetInternal" }
            - { type: "null" }

    SurveyAsset:
      type: object
      description: Full admin-only view of a survey asset row.
      additionalProperties: true
      required: [id, partnerId, name, approvalStatus, materialOverrideMode]
      properties:
        id: { type: integer }
        partnerId: { type: integer }
        externalAssetId: { type: ["string", "null"] }
        name: { type: string }
        description: { type: ["string", "null"] }
        approvalStatus: { type: string, enum: [pending, approved, rejected] }
        materialOverrideMode: { type: string, enum: [per_item, global, custom] }
        publicPhotoUrl: { type: ["string", "null"] }
        approvedMaterialsJson:
          type: ["array", "null"]
          items: { type: string }
        customApprovedMaterialsJson:
          type: ["array", "null"]
          items: { type: string }

    PublicSurveyAsset:
      type: object
      description: Customer-safe projection — guaranteed to omit measurements and all internal fields. Mirrors `toPublicSurveyAsset()` in lib/db.
      additionalProperties: false
      required: [id, externalAssetId, name, publicPhotos, approvedMaterials, materialOverrideMode]
      properties:
        id: { type: integer }
        externalAssetId: { type: string }
        name: { type: string }
        description: { type: ["string", "null"] }
        category: { type: ["string", "null"] }
        venueName: { type: ["string", "null"] }
        cityName: { type: ["string", "null"] }
        publicPhotoUrl: { type: ["string", "null"] }
        publicPhotos:
          type: array
          items: { $ref: "#/components/schemas/SurveyAssetPhoto" }
        approvedMaterials:
          type: array
          items: { type: string }
        materialOverrideMode:
          type: string
          enum: [per_item, global, custom]
        publicUseCase: { type: ["string", "null"] }

    SurveyAssetPatch:
      type: object
      additionalProperties: false
      properties:
        approvalStatus: { type: string, enum: [pending, approved, rejected] }
        rejectedReason: { type: ["string", "null"] }
        isActive: { type: boolean }
        name: { type: string }
        description: { type: ["string", "null"] }
        category: { type: ["string", "null"] }
        publicPhotoUrl: { type: ["string", "null"] }
        approvedMaterialsJson:
          type: ["array", "null"]
          items: { type: string }
        customApprovedMaterialsJson:
          type: ["array", "null"]
          items: { type: string }
        materialOverrideMode: { type: string, enum: [per_item, global, custom] }
        internalNotes: { type: ["string", "null"] }
        installNotes: { type: ["string", "null"] }
        productionNotes: { type: ["string", "null"] }

    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          type: string

    Partner:
      type: object
      required: [id, companyName, slug, isActive, createdAt, updatedAt]
      additionalProperties: true
      properties:
        id:
          type: integer
        companyName:
          type: string
        slug:
          type: string
        logoUrl:
          type: ["string", "null"]
        secondaryLogoUrl:
          type: ["string", "null"]
        websiteUrl:
          type: ["string", "null"]
        smallA3BadgeEnabled:
          type: boolean
        introHeadline:
          type: ["string", "null"]
        introText:
          type: ["string", "null"]
        thankYouText:
          type: ["string", "null"]
        capabilitiesLink:
          type: ["string", "null"]
        contactName:
          type: ["string", "null"]
        contactEmail:
          type: ["string", "null"]
        contactPhone:
          type: ["string", "null"]
        routingEmail:
          type: ["string", "null"]
        venueAddress:
          type: ["string", "null"]
        industryFocus:
          type: ["string", "null"]
        useCaseOptionsJson:
          type: ["array", "null"]
          items:
            type: string
        globalSizzleReelUrl:
          type: ["string", "null"]
        partnerVideoUrl:
          type: ["string", "null"]
        partnerDeckFileUrl:
          type: ["string", "null"]
        siteSurveyDeckFileUrl:
          type: ["string", "null"]
        portalMode:
          type: ["string", "null"]
        partnerType:
          type: ["string", "null"]
        unitPreference:
          type: ["string", "null"]
        pricingDisplayEnabled:
          type: boolean
        isActive:
          type: boolean
        defaultBillingExecModel:
          type: ["string", "null"]
        billingEntityName:
          type: ["string", "null"]
        paymentTerms:
          type: ["string", "null"]
        depositRequired:
          type: boolean
        depositPct:
          type: ["string", "null"]
        allowPartialPayment:
          type: boolean
        allowOrderOverride:
          type: boolean
        defaultBillingNotes:
          type: ["string", "null"]
        billingContactName:
          type: ["string", "null"]
        billingContactEmail:
          type: ["string", "null"]
        billingContactPhone:
          type: ["string", "null"]
        billingActive:
          type: boolean
        emailFromName:
          type: ["string", "null"]
        replyToEmail:
          type: ["string", "null"]
        emailSenderLabel:
          type: ["string", "null"]
        internalForwardEmail:
          type: ["string", "null"]
        ccEmail:
          type: ["string", "null"]
        emailEnabled:
          type: boolean
        attachPdfCustomer:
          type: boolean
        attachPdfOps:
          type: boolean
        attachPdfFinance:
          type: boolean
        attachPdfPartnerContact:
          type: boolean
        defaultCurrency:
          type: ["string", "null"]
        defaultTaxMode:
          type: ["string", "null"]
        defaultTaxLabel:
          type: ["string", "null"]
        defaultTaxRate:
          type: ["string", "null"]
        taxInclusive:
          type: boolean
        billingCountry:
          type: ["string", "null"]
        invoiceDisplayNotes:
          type: ["string", "null"]
        addonDisplayFormat:
          type: ["string", "null"]
        addonCategoryGroupingEnabled:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreatePartnerBody:
      type: object
      required: [companyName, slug]
      additionalProperties: true
      properties:
        companyName:
          type: string
        slug:
          type: string
        logoUrl:
          type: ["string", "null"]
        secondaryLogoUrl:
          type: ["string", "null"]
        websiteUrl:
          type: ["string", "null"]
        smallA3BadgeEnabled:
          type: boolean
        introHeadline:
          type: ["string", "null"]
        introText:
          type: ["string", "null"]
        thankYouText:
          type: ["string", "null"]
        capabilitiesLink:
          type: ["string", "null"]
        contactName:
          type: ["string", "null"]
        contactEmail:
          type: ["string", "null"]
        contactPhone:
          type: ["string", "null"]
        routingEmail:
          type: ["string", "null"]
        venueAddress:
          type: ["string", "null"]
        industryFocus:
          type: ["string", "null"]
        useCaseOptionsJson:
          type: ["array", "null"]
          items:
            type: string
        globalSizzleReelUrl:
          type: ["string", "null"]
        partnerVideoUrl:
          type: ["string", "null"]
        partnerDeckFileUrl:
          type: ["string", "null"]
        siteSurveyDeckFileUrl:
          type: ["string", "null"]
        portalMode:
          type: string
        partnerType:
          type: ["string", "null"]
        unitPreference:
          type: ["string", "null"]
        pricingDisplayEnabled:
          type: boolean
        isActive:
          type: boolean
        defaultBillingExecModel:
          type: ["string", "null"]
        billingEntityName:
          type: ["string", "null"]
        paymentTerms:
          type: ["string", "null"]
        depositRequired:
          type: boolean
        depositPct:
          type: ["string", "null"]
        allowPartialPayment:
          type: boolean
        allowOrderOverride:
          type: boolean
        defaultBillingNotes:
          type: ["string", "null"]
        billingContactName:
          type: ["string", "null"]
        billingContactEmail:
          type: ["string", "null"]
        billingContactPhone:
          type: ["string", "null"]
        billingActive:
          type: boolean
        emailFromName:
          type: ["string", "null"]
        replyToEmail:
          type: ["string", "null"]
        emailSenderLabel:
          type: ["string", "null"]
        internalForwardEmail:
          type: ["string", "null"]
        ccEmail:
          type: ["string", "null"]
        emailEnabled:
          type: boolean
        attachPdfCustomer:
          type: boolean
        attachPdfOps:
          type: boolean
        attachPdfFinance:
          type: boolean
        attachPdfPartnerContact:
          type: boolean
        defaultCurrency:
          type: string
        defaultTaxMode:
          type: string
        defaultTaxLabel:
          type: ["string", "null"]
        defaultTaxRate:
          type: ["string", "null"]
        taxInclusive:
          type: boolean
        billingCountry:
          type: ["string", "null"]
        invoiceDisplayNotes:
          type: ["string", "null"]
        addonDisplayFormat:
          type: string
        addonCategoryGroupingEnabled:
          type: boolean

    UpdatePartnerBody:
      type: object
      additionalProperties: true
      properties:
        companyName:
          type: string
        slug:
          type: string
        logoUrl:
          type: ["string", "null"]
        secondaryLogoUrl:
          type: ["string", "null"]
        websiteUrl:
          type: ["string", "null"]
        smallA3BadgeEnabled:
          type: boolean
        introHeadline:
          type: ["string", "null"]
        introText:
          type: ["string", "null"]
        thankYouText:
          type: ["string", "null"]
        capabilitiesLink:
          type: ["string", "null"]
        contactName:
          type: ["string", "null"]
        contactEmail:
          type: ["string", "null"]
        contactPhone:
          type: ["string", "null"]
        routingEmail:
          type: ["string", "null"]
        venueAddress:
          type: ["string", "null"]
        industryFocus:
          type: ["string", "null"]
        useCaseOptionsJson:
          type: ["array", "null"]
          items:
            type: string
        globalSizzleReelUrl:
          type: ["string", "null"]
        partnerVideoUrl:
          type: ["string", "null"]
        partnerDeckFileUrl:
          type: ["string", "null"]
        siteSurveyDeckFileUrl:
          type: ["string", "null"]
        portalMode:
          type: ["string", "null"]
        partnerType:
          type: ["string", "null"]
        unitPreference:
          type: ["string", "null"]
        pricingDisplayEnabled:
          type: boolean
        isActive:
          type: boolean
        defaultBillingExecModel:
          type: ["string", "null"]
        billingEntityName:
          type: ["string", "null"]
        paymentTerms:
          type: ["string", "null"]
        depositRequired:
          type: boolean
        depositPct:
          type: ["string", "null"]
        allowPartialPayment:
          type: boolean
        allowOrderOverride:
          type: boolean
        defaultBillingNotes:
          type: ["string", "null"]
        billingContactName:
          type: ["string", "null"]
        billingContactEmail:
          type: ["string", "null"]
        billingContactPhone:
          type: ["string", "null"]
        billingActive:
          type: boolean
        emailFromName:
          type: ["string", "null"]
        replyToEmail:
          type: ["string", "null"]
        emailSenderLabel:
          type: ["string", "null"]
        internalForwardEmail:
          type: ["string", "null"]
        ccEmail:
          type: ["string", "null"]
        emailEnabled:
          type: boolean
        attachPdfCustomer:
          type: boolean
        attachPdfOps:
          type: boolean
        attachPdfFinance:
          type: boolean
        attachPdfPartnerContact:
          type: boolean
        defaultCurrency:
          type: string
        defaultTaxMode:
          type: string
        defaultTaxLabel:
          type: ["string", "null"]
        defaultTaxRate:
          type: ["string", "null"]
        taxInclusive:
          type: boolean
        billingCountry:
          type: ["string", "null"]
        invoiceDisplayNotes:
          type: ["string", "null"]
        addonDisplayFormat:
          type: string
        addonCategoryGroupingEnabled:
          type: boolean

    PartnerAsset:
      type: object
      required: [id, partnerId, assetType, fileUrl, fileName, createdAt]
      properties:
        id:
          type: integer
        partnerId:
          type: integer
        assetType:
          type: string
        fileUrl:
          type: string
        fileName:
          type: string
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    CreatePartnerAssetBody:
      type: object
      required: [assetType, fileUrl, fileName]
      properties:
        assetType:
          type: string
        fileUrl:
          type: string
        fileName:
          type: string
        notes:
          type: string

    PublicPartner:
      type: object
      required: [id, companyName, slug, isActive]
      properties:
        id:
          type: integer
        companyName:
          type: string
        slug:
          type: string
        logoUrl:
          type: ["string", "null"]
        smallA3BadgeEnabled:
          type: boolean
        introHeadline:
          type: ["string", "null"]
        introText:
          type: ["string", "null"]
        contactName:
          type: ["string", "null"]
        contactEmail:
          type: ["string", "null"]
        contactPhone:
          type: ["string", "null"]
        venueAddress:
          type: ["string", "null"]
        industryFocus:
          type: ["string", "null"]
        useCaseOptionsJson:
          type: ["array", "null"]
          items:
            type: string
        globalSizzleReelUrl:
          type: ["string", "null"]
        partnerVideoUrl:
          type: ["string", "null"]
        pricingDisplayEnabled:
          type: boolean
        isActive:
          type: boolean
        pricingRules:
          type: array
          items:
            $ref: "#/components/schemas/PublicPricingItem"

    SubmitRequestBody:
      type: object
      required: [companyName, contactName, email, eventName]
      properties:
        companyName:
          type: string
        contactName:
          type: string
        email:
          type: string
        phone:
          type: string
        eventName:
          type: string
        eventDate:
          type: string
        venueName:
          type: string
        venueAddress:
          type: string
        installDatetime:
          type: string
        removalDatetime:
          type: string
        postEventDisposition:
          type: string
        industry:
          type: string
        useCase:
          type: string
        designAssistanceRequested:
          type: boolean
        customFabricationRequested:
          type: boolean
        immersiveRequested:
          type: boolean
        promotionalItemsRequested:
          type: boolean
        additionalNotes:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/RequestItemInput"
        uploads:
          type: array
          items:
            $ref: "#/components/schemas/RequestUploadInput"

    RequestItemInput:
      type: object
      required: [category, itemName]
      properties:
        category:
          type: string
        itemName:
          type: string
        quantityNote:
          type: string
        sizeNote:
          type: string
        width:
          type: number
          nullable: true
        height:
          type: number
          nullable: true
        sizeUnit:
          type: string
          nullable: true
          enum: [in, ft, mm, cm, m]
        productId:
          type: integer
          nullable: true
        brandingZoneId:
          type: integer
          nullable: true

    RequestUploadInput:
      type: object
      required: [uploadType, fileUrl, fileName]
      properties:
        uploadType:
          type: string
        fileUrl:
          type: string
        fileName:
          type: string
        mimeType:
          type: string

    SubmitRequestResponse:
      type: object
      required: [id, message]
      properties:
        id:
          type: integer
        message:
          type: string

    RequestSummary:
      type: object
      required: [id, partnerName, companyName, contactName, eventName, status, createdAt]
      properties:
        id:
          type: integer
        partnerId:
          type: integer
        partnerName:
          type: string
        companyName:
          type: string
        contactName:
          type: string
        email:
          type: string
        eventName:
          type: string
        eventDate:
          type: ["string", "null"]
        status:
          type: string
        estimatedScopeLevel:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    RequestListResponse:
      type: object
      required: [requests, total]
      properties:
        requests:
          type: array
          items:
            $ref: "#/components/schemas/RequestSummary"
        total:
          type: integer

    RequestDetail:
      type: object
      required: [id, partnerId, companyName, contactName, email, eventName, status, createdAt]
      properties:
        id:
          type: integer
        partnerId:
          type: integer
        partnerName:
          type: string
        companyName:
          type: string
        contactName:
          type: string
        email:
          type: string
        phone:
          type: ["string", "null"]
        eventName:
          type: string
        eventDate:
          type: ["string", "null"]
        venueName:
          type: ["string", "null"]
        venueAddress:
          type: ["string", "null"]
        installDatetime:
          type: ["string", "null"]
        removalDatetime:
          type: ["string", "null"]
        postEventDisposition:
          type: ["string", "null"]
        industry:
          type: ["string", "null"]
        useCase:
          type: ["string", "null"]
        designAssistanceRequested:
          type: boolean
        customFabricationRequested:
          type: boolean
        immersiveRequested:
          type: boolean
        promotionalItemsRequested:
          type: boolean
        additionalNotes:
          type: ["string", "null"]
        status:
          type: string
        aiSummary:
          type: ["string", "null"]
        internalSummary:
          type: ["string", "null"]
        estimatedScopeLevel:
          type: ["string", "null"]
        recommendedUpsellsJson:
          type: ["array", "null"]
          items:
            type: string
        pdfSummaryUrl:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        items:
          type: array
          items:
            $ref: "#/components/schemas/RequestItem"
        uploads:
          type: array
          items:
            $ref: "#/components/schemas/RequestUpload"
        notes:
          type: array
          items:
            $ref: "#/components/schemas/AdminNote"

    RequestItem:
      type: object
      required: [id, requestId, category, itemName]
      properties:
        id:
          type: integer
        requestId:
          type: integer
        category:
          type: string
        itemName:
          type: string
        quantityNote:
          type: ["string", "null"]
        sizeNote:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    RequestUpload:
      type: object
      required: [id, requestId, uploadType, fileUrl, fileName]
      properties:
        id:
          type: integer
        requestId:
          type: integer
        uploadType:
          type: string
        fileUrl:
          type: string
        fileName:
          type: string
        mimeType:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    AdminNote:
      type: object
      required: [id, requestId, noteBody, createdAt]
      properties:
        id:
          type: integer
        requestId:
          type: integer
        noteBody:
          type: string
        createdAt:
          type: string
          format: date-time

    CreateNoteBody:
      type: object
      required: [noteBody]
      properties:
        noteBody:
          type: string

    UpdateRequestBody:
      type: object
      properties:
        status:
          type: string
        internalSummary:
          type: ["string", "null"]

    PricingRule:
      type: object
      required: [id, category, itemName, isActive, createdAt]
      properties:
        id:
          type: integer
        category:
          type: string
        itemName:
          type: string
        startingPrice:
          type: ["number", "null"]
        internalCostBasis:
          type: ["number", "null"]
        rushFeeRule:
          type: ["string", "null"]
        installFeeRule:
          type: ["string", "null"]
        removalFeeRule:
          type: ["string", "null"]
        designFeeRule:
          type: ["string", "null"]
        upsellTagsJson:
          type: ["array", "null"]
          items:
            type: string
        isActive:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreatePricingRuleBody:
      type: object
      required: [category, itemName]
      properties:
        category:
          type: string
        itemName:
          type: string
        startingPrice:
          type: number
        internalCostBasis:
          type: number
        rushFeeRule:
          type: string
        installFeeRule:
          type: string
        removalFeeRule:
          type: string
        designFeeRule:
          type: string
        upsellTagsJson:
          type: array
          items:
            type: string
        isActive:
          type: boolean

    UpdatePricingRuleBody:
      type: object
      properties:
        category:
          type: string
        itemName:
          type: string
        startingPrice:
          type: ["number", "null"]
        internalCostBasis:
          type: ["number", "null"]
        rushFeeRule:
          type: ["string", "null"]
        installFeeRule:
          type: ["string", "null"]
        removalFeeRule:
          type: ["string", "null"]
        designFeeRule:
          type: ["string", "null"]
        upsellTagsJson:
          type: ["array", "null"]
          items:
            type: string
        isActive:
          type: boolean

    PublicPricingItem:
      type: object
      required: [category, itemName]
      properties:
        category:
          type: string
        itemName:
          type: string
        startingPrice:
          type: ["number", "null"]
        displayNote:
          type: ["string", "null"]

    PdfResponse:
      type: object
      required: [pdfUrl]
      properties:
        pdfUrl:
          type: string

    DashboardSummary:
      type: object
      required: [totalPartners, totalRequests, newRequestsToday, requestsByStatus]
      properties:
        totalPartners:
          type: integer
        activePartners:
          type: integer
        totalRequests:
          type: integer
        newRequestsToday:
          type: integer
        requestsByStatus:
          type: array
          items:
            $ref: "#/components/schemas/StatusCount"
        recentPartners:
          type: array
          items:
            $ref: "#/components/schemas/Partner"

    StatusCount:
      type: object
      required: [status, count]
      properties:
        status:
          type: string
        count:
          type: integer

    AssetsLibrary:
      type: object
      required: [partnerAssets, requestUploads]
      properties:
        partnerAssets:
          type: array
          items:
            $ref: "#/components/schemas/PartnerAssetGroup"
        requestUploads:
          type: array
          items:
            $ref: "#/components/schemas/RequestUploadGroup"

    PartnerAssetGroup:
      type: object
      required: [partnerId, partnerName, assets]
      properties:
        partnerId:
          type: integer
        partnerName:
          type: string
        assets:
          type: array
          items:
            $ref: "#/components/schemas/PartnerAsset"

    RequestUploadGroup:
      type: object
      required: [requestId, eventName, partnerName, uploads]
      properties:
        requestId:
          type: integer
        eventName:
          type: string
        partnerName:
          type: string
        uploads:
          type: array
          items:
            $ref: "#/components/schemas/RequestUpload"

    UploadUrlRequest:
      type: object
      required: [name, size, contentType]
      properties:
        name:
          type: string
          minLength: 1
        size:
          type: integer
          minimum: 1
        contentType:
          type: string
          minLength: 1

    UploadUrlResponse:
      type: object
      required: [uploadURL, objectPath]
      properties:
        uploadURL:
          type: string
          format: uri
        objectPath:
          type: string
        metadata:
          $ref: '#/components/schemas/UploadUrlRequest'

    SupplierPacketContact:
      type: object
      properties:
        name: { type: ["string", "null"] }
        phone: { type: ["string", "null"] }
        email: { type: ["string", "null"] }

    SupplierPacketDualValue:
      type: object
      description: A measurement rendered in its native unit, optionally with a converted secondary value.
      required: [primary, secondary, converted]
      properties:
        primary: { type: string }
        secondary: { type: ["string", "null"] }
        converted: { type: boolean }

    SupplierPacketItemSpecs:
      type: object
      description: Structured spec block for a line item — finished/artwork/visible/bleed/safe-area dimensions.
      required: [finished, artwork, visible, bleed, safeArea]
      properties:
        finished:
          oneOf:
            - $ref: "#/components/schemas/SupplierPacketDualValue"
            - type: "null"
        artwork:
          oneOf:
            - $ref: "#/components/schemas/SupplierPacketDualValue"
            - type: "null"
        visible:
          oneOf:
            - $ref: "#/components/schemas/SupplierPacketDualValue"
            - type: "null"
        bleed:
          oneOf:
            - $ref: "#/components/schemas/SupplierPacketDualValue"
            - type: "null"
        safeArea:
          oneOf:
            - $ref: "#/components/schemas/SupplierPacketDualValue"
            - type: "null"

    SupplierPacketPricingBasis:
      type: object
      additionalProperties: true
      required:
        [
          pricingModel,
          pricingUnit,
          pricingUnitLabel,
          unitRate,
          billableAreaSqm,
          billableLinearM,
          unitPrice,
          minBillableSize,
          minCharge,
          calculation,
        ]
      properties:
        pricingModel: { type: ["string", "null"] }
        pricingUnit: { type: ["string", "null"] }
        pricingUnitLabel: { type: ["string", "null"] }
        unitRate:
          oneOf:
            - { type: string }
            - { type: number }
            - { type: "null" }
        billableAreaSqm: { type: ["number", "null"] }
        billableLinearM: { type: ["number", "null"] }
        unitPrice:
          oneOf:
            - { type: string }
            - { type: number }
            - { type: "null" }
        minBillableSize: { type: ["number", "null"] }
        minCharge:
          oneOf:
            - { type: string }
            - { type: number }
            - { type: "null" }
        calculation: { type: ["string", "null"] }
        requiresQuote: { type: boolean }

    SupplierPacketAsset:
      type: object
      description: Full asset row (vendor-visible, current, approved). Date fields are ISO strings.
      additionalProperties: true
      required: [id, title, fileUrl, version, isCurrent, status, approvalStatus]
      properties:
        id: { type: integer }
        title: { type: string }
        fileUrl: { type: string }
        fileName: { type: ["string", "null"] }
        mimeType: { type: ["string", "null"] }
        fileSize: { type: ["integer", "null"] }
        category: { type: string }
        visibility: { type: string }
        ownerType: { type: ["string", "null"] }
        ownerId: { type: ["integer", "null"] }
        partnerId: { type: ["integer", "null"] }
        eventId: { type: ["integer", "null"] }
        orderId: { type: ["integer", "null"] }
        productId: { type: ["integer", "null"] }
        packageId: { type: ["integer", "null"] }
        brandingZoneId: { type: ["integer", "null"] }
        supplierId: { type: ["integer", "null"] }
        version: { type: integer }
        isCurrent: { type: boolean }
        parentAssetId: { type: ["integer", "null"] }
        status: { type: string }
        approvalStatus: { type: string }
        approvedByUserId: { type: ["string", "null"] }
        approvedAt: { type: ["string", "null"], format: date-time }
        releasedToVendorAt: { type: ["string", "null"], format: date-time }
        productionReady: { type: boolean }
        uploadedByUserId: { type: ["string", "null"] }
        notes: { type: ["string", "null"] }
        tagsJson:
          type: ["array", "null"]
          items: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    SupplierPacketAssetLink:
      type: object
      description: Asset-link row joined to its asset (or null when the asset is not vendor-visible).
      additionalProperties: true
      required: [id, assetId, orderItemId, isRequiredFor, createdAt, asset]
      properties:
        id: { type: integer }
        assetId: { type: integer }
        orderItemId: { type: integer }
        role: { type: ["string", "null"] }
        isRequiredFor: { type: boolean }
        notes: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        asset:
          oneOf:
            - $ref: "#/components/schemas/SupplierPacketAsset"
            - type: "null"
        hidden: { type: boolean }

    SupplierPacketItem:
      type: object
      required:
        [
          itemId,
          name,
          productId,
          productName,
          quantity,
          dimensionDisplay,
          specs,
          pricingBasis,
          fulfillmentMode,
          supplierStatus,
          supplierDueDate,
          supplierShipDate,
          supplierInstallDate,
          internalFulfillmentNotes,
          productionBlockedReason,
          assets,
          flags,
          ready,
        ]
      properties:
        itemId: { type: integer }
        name: { type: string }
        productId: { type: ["integer", "null"] }
        productName: { type: ["string", "null"] }
        quantity: { type: integer }
        dimensionDisplay: { type: ["string", "null"] }
        specs:
          oneOf:
            - $ref: "#/components/schemas/SupplierPacketItemSpecs"
            - type: "null"
        pricingBasis:
          oneOf:
            - $ref: "#/components/schemas/SupplierPacketPricingBasis"
            - type: "null"
        fulfillmentMode: { type: ["string", "null"] }
        supplierStatus: { type: ["string", "null"] }
        supplierDueDate: { type: ["string", "null"], format: date-time }
        supplierShipDate: { type: ["string", "null"], format: date-time }
        supplierInstallDate: { type: ["string", "null"], format: date-time }
        internalFulfillmentNotes: { type: ["string", "null"] }
        productionBlockedReason: { type: ["string", "null"] }
        assets:
          type: array
          items: { $ref: "#/components/schemas/SupplierPacketAssetLink" }
        flags:
          type: array
          items: { type: string }
        ready: { type: boolean }

    SupplierPacketOrder:
      type: object
      required:
        [
          id,
          orderNumber,
          status,
          internalNotes,
          vendorNotes,
          shipDateTarget,
          deliveryByDate,
          packageCount,
          totalShipmentWeight,
          totalShipmentWeightUnit,
          oversizeFlag,
          crateRequired,
          palletRequired,
          shippingContactJson,
          receivingContactJson,
          customsNotes,
          internationalShippingNotes,
          logisticsNotes,
          measurementSystem,
        ]
      properties:
        id: { type: integer }
        orderNumber: { type: ["string", "null"] }
        status: { type: ["string", "null"] }
        internalNotes: { type: ["string", "null"] }
        vendorNotes: { type: ["string", "null"] }
        shipDateTarget: { type: ["string", "null"], format: date-time }
        deliveryByDate: { type: ["string", "null"], format: date-time }
        packageCount: { type: ["integer", "null"] }
        totalShipmentWeight:
          oneOf:
            - { type: string }
            - { type: number }
            - { type: "null" }
        totalShipmentWeightUnit: { type: ["string", "null"] }
        oversizeFlag: { type: ["boolean", "null"] }
        crateRequired: { type: ["boolean", "null"] }
        palletRequired: { type: ["boolean", "null"] }
        shippingContactJson:
          oneOf:
            - $ref: "#/components/schemas/SupplierPacketContact"
            - type: "null"
        receivingContactJson:
          oneOf:
            - $ref: "#/components/schemas/SupplierPacketContact"
            - type: "null"
        customsNotes: { type: ["string", "null"] }
        internationalShippingNotes: { type: ["string", "null"] }
        logisticsNotes: { type: ["string", "null"] }
        measurementSystem: { type: ["string", "null"] }

    SupplierPacketPartner:
      oneOf:
        - type: object
          required: [id, companyName]
          properties:
            id: { type: integer }
            companyName: { type: string }
        - type: "null"

    SupplierPacketEvent:
      oneOf:
        - type: object
          required: [id, name, eventStartDate, eventEndDate, venueId]
          properties:
            id: { type: integer }
            name: { type: string }
            eventStartDate: { type: ["string", "null"], format: date-time }
            eventEndDate: { type: ["string", "null"], format: date-time }
            venueId: { type: ["integer", "null"] }
        - type: "null"

    SupplierPacketSupplier:
      type: object
      required: [id, name]
      properties:
        id: { type: integer }
        name: { type: string }

    SupplierPacketMeasurementContext:
      type: object
      required: [system, source, reason]
      properties:
        system: { type: string }
        primarySystem: { type: string }
        secondarySystem: { type: string }
        source: { type: string }
        reason: { type: string }

    SupplierPacketSummary:
      type: object
      required: [totalItems, ready, blocked]
      properties:
        totalItems: { type: integer }
        ready: { type: integer }
        blocked: { type: integer }

    SupplierPacketResponse:
      type: object
      required: [order, partner, event, supplier, items, orderLevelAssets, summary]
      properties:
        order: { $ref: "#/components/schemas/SupplierPacketOrder" }
        partner: { $ref: "#/components/schemas/SupplierPacketPartner" }
        event: { $ref: "#/components/schemas/SupplierPacketEvent" }
        supplier: { $ref: "#/components/schemas/SupplierPacketSupplier" }
        items:
          type: array
          items: { $ref: "#/components/schemas/SupplierPacketItem" }
        measurementContext:
          $ref: "#/components/schemas/SupplierPacketMeasurementContext"
        orderLevelAssets:
          type: array
          items: { $ref: "#/components/schemas/SupplierPacketAsset" }
        summary: { $ref: "#/components/schemas/SupplierPacketSummary" }

    OrderReadinessSummary:
      type: object
      required: [total, ready, blocked, missingArtwork, missingProof, awaitingApproval]
      properties:
        total: { type: integer }
        ready: { type: integer }
        blocked: { type: integer }
        missingArtwork: { type: integer }
        missingProof: { type: integer }
        awaitingApproval: { type: integer }

    OrderReadinessExpectations:
      type: object
      required: [needsArtwork, needsProof]
      properties:
        needsArtwork: { type: boolean }
        needsProof: { type: boolean }

    OrderReadinessAssetLink:
      type: object
      additionalProperties: true
      required: [linkId, role, asset]
      properties:
        linkId: { type: integer }
        role: { type: ["string", "null"] }
        asset: { $ref: "#/components/schemas/SupplierPacketAsset" }

    OrderReadinessItem:
      type: object
      additionalProperties: true
      required:
        [
          itemId,
          name,
          itemType,
          quantity,
          productId,
          brandingZoneId,
          assignedSupplierId,
          supplierStatus,
          fulfillmentMode,
          expectations,
          assets,
          currentArtworkAssetId,
          approvedArtworkAssetId,
          flags,
          productionReady,
          productionBlockedReason,
        ]
      properties:
        itemId: { type: integer }
        name: { type: ["string", "null"] }
        itemType: { type: ["string", "null"] }
        quantity: { type: ["integer", "null"] }
        productId: { type: ["integer", "null"] }
        brandingZoneId: { type: ["integer", "null"] }
        assignedSupplierId: { type: ["integer", "null"] }
        supplierStatus: { type: ["string", "null"] }
        fulfillmentMode: { type: ["string", "null"] }
        expectations: { $ref: "#/components/schemas/OrderReadinessExpectations" }
        assets:
          type: array
          items: { $ref: "#/components/schemas/OrderReadinessAssetLink" }
        currentArtworkAssetId: { type: ["integer", "null"] }
        approvedArtworkAssetId: { type: ["integer", "null"] }
        flags:
          type: array
          items: { type: string }
        productionReady: { type: boolean }
        productionBlockedReason: { type: ["string", "null"] }

    OrderReadinessResponse:
      type: object
      required: [orderId, items, summary]
      properties:
        orderId: { type: integer }
        items:
          type: array
          items: { $ref: "#/components/schemas/OrderReadinessItem" }
        summary: { $ref: "#/components/schemas/OrderReadinessSummary" }

    OrderItemProductionBlockResponse:
      type: object
      additionalProperties: true
      description: |
        Updated order item row. Only the most stable identifying fields are
        listed as required; the row carries many additional columns that are
        passed through to the client.
      required: [id, orderId]
      properties:
        id: { type: integer }
        orderId: { type: integer }
        productionBlockedReason: { type: ["string", "null"] }

    ProductionDashboardCounters:
      type: object
      required:
        [awaitingReview, awaitingApproval, approved, vendorReleased, revisionRequested, superseded]
      properties:
        awaitingReview: { type: integer }
        awaitingApproval: { type: integer }
        approved: { type: integer }
        vendorReleased: { type: integer }
        revisionRequested: { type: integer }
        superseded: { type: integer }

    ProductionDashboardOrderIssue:
      type: object
      additionalProperties: true
      required:
        [
          orderId,
          orderNumber,
          partnerId,
          total,
          ready,
          blocked,
          missingArtwork,
          missingProof,
          awaitingApproval,
        ]
      properties:
        orderId: { type: integer }
        orderNumber: { type: ["string", "null"] }
        partnerId: { type: ["integer", "null"] }
        total: { type: integer }
        ready: { type: integer }
        blocked: { type: integer }
        missingArtwork: { type: integer }
        missingProof: { type: integer }
        awaitingApproval: { type: integer }

    ProductionDashboardResponse:
      type: object
      required: [counters, latest, byEvent, bySupplier, orderIssues]
      properties:
        counters: { $ref: "#/components/schemas/ProductionDashboardCounters" }
        latest:
          type: array
          items: { $ref: "#/components/schemas/SupplierPacketAsset" }
        byEvent:
          type: object
          additionalProperties: { type: integer }
        bySupplier:
          type: object
          additionalProperties: { type: integer }
        orderIssues:
          type: array
          items: { $ref: "#/components/schemas/ProductionDashboardOrderIssue" }
