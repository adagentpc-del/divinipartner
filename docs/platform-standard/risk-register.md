# Risk Register

Unresolved risks, severity, owner, mitigation, target state. Bootstrapped at
Section 01 from findings surfaced during discovery; will grow with each
later section.

| Risk ID | Description | Severity | Owner | Current Mitigation | Target State | Section |
|---|---|---|---|---|---|---|
| R-01 | **RESOLVED 2026-08-09.** No age-affirmation step at registration | Was Low (hygiene, not a live violation given non-child-directed positioning) | N/A — fixed | Server-side `ageConfirmed === true` requirement on `POST /register` (not just a client checkbox) plus a matching UI checkbox next to the existing Terms acceptance; live-verified against a real database (missing/false both 400, true succeeds) | N/A — resolved | 01 (COPPA hygiene row), closed in Section 17 |
| R-02 | `audit_logs` retention period not centrally defined | Low | Owner | Table exists and is actively written; no automatic purge/retention policy found | Define retention in the data-retention matrix (Section 02) and enforce it | 01 → 02/06 |
| R-03 | Money-transmission / marketplace-facilitator sales-tax / 1099 exposure not yet resolved by counsel | Medium (blocks T7, not a current live-money risk since Stripe is unconfigured) | Owner + counsel | `STRIPE_SECRET_KEY` intentionally unset — no real money moves today | Counsel review before T7 unblocks | 01 → 09/17 (already tracked as T7/T8 in `AI_PROJECT_OS/12_TASK_QUEUE.md`) |
| R-04 | No independent penetration test performed | Medium | Owner | Internal adversarial testing exists for several controls (CSRF, IDOR-style checks noted in `AI_PROJECT_OS/51_SECURITY.md`) but no third-party pen test | Commission an independent pen test before T7 (real money) or first enterprise deal | 01 → 15/18 |
| R-05 | No formal SOC 2 / ISO 27001 certification (technical controls only) | Low (market-driven, not legally required today) | Owner | Technical-controls audit complete and gaps closed (`AI_PROJECT_OS/53_SOC2_ISO27001_AUDIT.md`) | Formal engagement once there's a customer/contract driver | 01 → 18 |
| R-06 | No separate staging environment | Low–Medium | Owner | Local dev + production only; CI runs typecheck/tests before merge | Consider a staging environment before major schema/payment changes ship directly to production | 01 → 03 |
| R-07 | Backup cron job not yet installed on the production server; error-monitoring webhook not yet pointed at a real destination | Medium | Owner | Both mechanisms are built and tested (`AI_PROJECT_OS/23_DEPLOYMENT.md`, `51_SECURITY.md`); only the operator step remains | Install the cron job; set `ERROR_MONITORING_WEBHOOK_URL` | 01 → 06/14 (already tracked as operator actions in `51_SECURITY.md`) |
| R-08 | **RESOLVED 2026-08-08.** Privacy self-service tool (access/export/correction/deletion requests, consent management) existed and worked correctly but had no discoverable path for ordinary users — admin-sounding URL only, no nav link, and the Privacy Policy told users to email support instead of mentioning it | Was Medium | N/A — fixed | `/account/privacy` route added, linked from Profile → Account, Privacy.tsx updated | N/A — resolved, live-verified | 02 |
| R-09 | `data_retention_policies` mechanism works but zero policies are declared, and nothing enforces declared policies automatically even once set (declaration-only by design) | Medium | Owner/counsel | `docs/platform-standard/data-retention-matrix.md` proposes content | Owner/counsel adopts proposed periods; build enforcement jobs per category as warranted | 02 (T14, T15) |
| R-10 | `visitor_signals` (device/usage-signal collection) grows unbounded — no purge job exists despite the data being consent-gated and disclosed | Low–Medium | Owner | None today | Decide a retention window and build a purge job (T15) | 02 |
| R-11 | No DMCA/copyright-takedown notice despite hosting UGC; no AI-use disclosure despite real AI features; no Accessibility Statement despite ADA applicability | Medium (DMCA, AI disclosure); Low (Accessibility Statement, sequenced after Section 11) | Owner/counsel | None today | Add three DRAFT pages pending counsel review (T16, T17, T18) | 02 |
| R-12 | **PARTIALLY RESOLVED 2026-08-08.** CI did not enforce locked dependency installs (`npm install`, not `npm ci`), never ran an actual build (only typechecked), and had no dependency-vulnerability gate or automated update alerts. | Was Medium | N/A — fixed | `npm ci` + real build steps + `npm audit --omit=dev` (server) + `.github/dependabot.yml`, all verified locally before commit | N/A — resolved for server; root/SPA audit gate deferred (R-13) | 03 |
| R-13 | 11 `npm audit` findings (3 moderate, 7 high, 1 critical) in root, all traced to Capacitor mobile-build-only devDependencies (never installed on the production server; confirmed via `deploy.sh`) | Low real-world severity, but unresolved | Owner (needs Mac/Xcode to verify a `--force` fix doesn't break mobile builds) | Safe non-forcing `npm audit fix` already applied | Resolve via T19 once mobile-build verification is possible | 03 |
| R-14 | **PARTIALLY RESOLVED 2026-08-08.** Deploy is a manual `rsync` + `deploy.sh` loop with no technical gate requiring code to have passed CI/PR review first; no `CODEOWNERS`; no tagged releases; branch-protection settings unconfirmed | Was Low-Medium | N/A — mostly fixed | `CODEOWNERS` added; `v0.1.0` tag created locally (not yet published -- push rejected) | Branch-protection confirmation remains an operator action (GitHub-UI setting, not visible from repo contents) | 03 |
| R-15 | **RESOLVED 2026-08-08.** No written secrets-rotation runbook (secrets themselves were already handled correctly; the *procedure* for a rotation event wasn't written down) | Was Low | N/A — fixed | `compliance/policies/secrets-rotation-runbook.md` | N/A — resolved (DRAFT status pending a named owner, same as every other document in that folder) | 03 |
| R-16 | **RESOLVED 2026-08-08.** No linter configured at all | Was Medium | N/A — fixed | ESLint installed, configured, wired into CI, real findings fixed (one Rules-of-Hooks bug, one unstable-key anti-pattern) | N/A — resolved | 03 |
| R-17 | **RESOLVED 2026-08-08.** Privacy Policy claimed "database row-level security" for data protection; no Postgres RLS actually exists anywhere in the schema (authorization is entirely application-layer) | Was Low-Medium (misleading claim, not a security hole) | N/A — fixed | `src/pages/Privacy.tsx` reworded to accurately describe the real application-layer scoping mechanism | N/A — resolved | 05 |
| R-18 | No Postgres Row-Level Security in use anywhere; the entire multi-tenant boundary is enforced in application code (`actor.org.id` scoping per route), with no DB-level defense-in-depth backstop | Medium (standing architectural risk, not a live exploit — live adversarial testing in Section 05 found zero cross-tenant leaks in the routes tested) | Owner/engineering | Consistent `getActor()` + per-route org-scoping pattern, confirmed sound via live testing of events/bids/quotes/invoices/signatures/decks/org-switching | Either adopt RLS as a second layer, or keep enforcing (and testing) the application-layer discipline as new org-scoped tables/routes are added | 05 |
| R-19 | **RESOLVED 2026-08-08.** Dead code: `objectStorage.ts` exported a working HMAC-signed presigned-download-URL mechanism (`signDownloadUrl`/`verifyDownloadUrl`) that no route actually mounted or called | Was Low | N/A — fixed | Removed the dead exports, their re-exports in `storage.ts`, the now-unused `DOWNLOAD_URL_SECRET` config var + its production startup checks, and updated `.env.local.example`/`DEPLOY.md` to stop referencing it; server boot verified clean after removal | N/A — resolved | 05 |
| R-20 | **RESOLVED 2026-08-08.** On subscription cancellation, `organizations.platform_fee_rate` was set to a flat value (`TIERS.free_partner.feeRate`) instead of the role-aware `planTierFor()` lookup the "active" branch uses; for roles like `client` whose catalog entry has `platformFeeRate: null`, this left a stale/incorrect cached value | Was Low | N/A — fixed | `applySubscriptionUpdate()`'s cancellation branch now uses the same `planTierFor(orgType, "free_partner")` lookup as the active branch | N/A — resolved | 05 |
| R-21 | **RESOLVED 2026-08-08.** 12 tables had an `organization_id` column with no index (2 more missing `user_id`); with no Postgres RLS, every tenant-scoped query is an app-layer `where organization_id = $1`, so these were on track to become full table scans as data grows, with `payments`/`platform_credits` on a money-adjacent read path | Was Medium (scale risk) | N/A — fixed | `db/schema-org-tenant-indexes.sql`, applied live, verified zero remaining gaps | N/A — resolved | 06 |
| R-22 | **RESOLVED 2026-08-08.** The backup/restore mechanism (built 2026-08-03) had never actually been exercised as a real restore, only unit-tested in isolation — "an untested backup is not a verified control" per the script's own top-of-file comment | Was Medium (unverified control) | N/A — fixed | Real end-to-end backup + restore performed against a disposable scratch database, exact table/row-count verification; `compliance/policies/backup-and-restore-runbook.md` written with RTO/RPO assumptions and honest gaps (production cron status, off-site storage config, no PITR — all remain operator-verification items) | N/A — resolved (mechanism verified; recurring operator rehearsal still needed, see operator-actions.md) | 06 |
| R-23 | **RESOLVED 2026-08-08.** `redeemCredit()` (`POST /api/credits/redeem`) checked balance then inserted the debit with no lock between the two — a real, reachable, money-adjacent double-spend race under concurrent requests | Was High (real double-spend on a live route, not theoretical) | N/A — fixed | Wrapped the check + insert in one transaction guarded by `pg_advisory_xact_lock(hashtext(userId))`; live-reproduced against a real test account before the fix (traced the exact race window) and live-verified after (10 concurrent $10 redemption requests against a $10 balance: exactly 1 succeeded, final balance exactly $0.00) | N/A — resolved | 06 |
| R-24 | The same check-then-insert race shape as R-23 exists in 5 entitlement-limit route files (`seats.ts`, `events.ts`, `inventory.ts`, `packages.ts`, `warehouses.ts` — all call `checkLimit()` with no lock between the usage count and the insert) | Low-Medium (soft plan-limit overrun only — at most a handful of extra free seats/events/items under concurrent-request abuse; not monetary, not cross-tenant, not a security bypass) | Engineering | None today | Apply the same `pg_advisory_xact_lock` pattern proven in `credits.ts` (lock keyed on `org.id`, recompute the count inside the same transaction as the insert) to all 5 route files | 06 |
| R-25 | 81 `*_id`-shaped columns across the schema have no FK constraint; the large majority are intentional (polymorphic `type`+`id` references, external Stripe ids), but a handful (partner/payout/exhibitor/sponsor tables) are real FK candidates once those flows carry live data — currently all empty since Stripe is unconfigured (T7) | Low today (zero live orphan risk — tables are empty); revisit once real data exists | Engineering | None needed today | Add real FK constraints to the partner/payout/exhibitor/sponsor tables before their first real write, once T7 unblocks | 06 |
| R-26 | **RESOLVED 2026-08-08.** `X-Powered-By: Express` leaked framework identity on every HTTP response | Was Low (minor info disclosure) | N/A — fixed | `app.disable("x-powered-by")`, confirmed live before/after | N/A — resolved | 07 |
| R-27 | **RESOLVED 2026-08-08.** `securityHeaders.ts` and `rateLimit.ts` both contained a stale "no MFA anywhere in this app" comment, false since Section 04 shipped native TOTP | Was Low (documentation accuracy) | N/A — fixed | Both files reworded to point at the real TOTP implementation instead of asserting its absence | N/A — resolved | 07 |
| R-28 | Uploaded images/PDFs retain original embedded metadata (EXIF GPS on photos, PDF author fields) — no stripping step exists | Low (current file mix is mostly business documents like W-9s/COIs and company logos, not personal photos with sensitive EXIF data) | Engineering | None today | Add a metadata-stripping step once a library is chosen for the app's actual file types | 07 |
| R-29 | **RESOLVED 2026-08-08.** AI extraction calls (website/document profile extraction) were not logged to `audit_logs`, inconsistent with every other privileged action in the codebase, and short of the pack's explicit `ai_run_audit` requirement | Was Low (observability gap, not exploitable — every AI output already required human review before touching a live record) | N/A — fixed | Both extraction routes now call `logAction("ai.extract_profile", ...)` with provider/model/source/outcome (never the extracted content); live-verified a real row was written | N/A — resolved | 08 |
| R-30 | No automated evaluation harness / golden test set exists to measure AI-extraction accuracy over time | Low (human-reviewed, non-hard-dependency feature; volume does not yet justify the investment) | Engineering | None today | Build a small eval harness once extraction volume/complaints justify the investment | 08 |
| R-31 | **RESOLVED 2026-08-08.** No event-level webhook idempotency/observability ledger existed; only payment-row-level idempotency, leaving several webhook event types with no dedup protection and no dead-letter/failure visibility | Was Medium | N/A — fixed | `webhook_events` table + `db/webhookEvents.ts`, wired into both Stripe and PayPal handlers; live-verified | N/A — resolved | 09 |
| R-32 | Subscription webhook processing has no out-of-order-delivery tolerance -- a sufficiently delayed, out-of-order event could in principle overwrite more-recent state | Low (narrow, self-healing in the common case) | Engineering | None today | Build a last-processed-event-timestamp guard if this becomes a real issue at volume | 09 |
| R-33 | No refund or dispute-response capability exists anywhere in the app -- no code path calls a refund API or handles a `charge.dispute.*` webhook | Medium once real money is live; none today (T7 still blocks real money, and current Payment Policy language is already consistent with this gap) | Engineering | None today | Build an admin-triggered refund action and a dispute webhook handler before T7 unblocks real money | 09 (T30) |
| R-34 | **RESOLVED 2026-08-08.** `sendEmail()`, the one shared transport for every email this app sends, had no bounce/complaint suppression check at all -- only the Claim Engine's outreach had its own narrower suppression list, and nothing anywhere auto-populated a bounce reason despite the schema supporting one | Was Medium (deliverability + compliance risk across the whole domain, not just claim outreach) | N/A — fixed | `communication_suppressions` table + `sendEmail()` per-recipient filter + a Svix-signed Resend bounce/complaint webhook; live-verified at every layer (signature rejection, DB matching, actual `sendEmail()` filtering behavior) | N/A — resolved | 10 |
| R-35 | No `List-Unsubscribe`/`List-Unsubscribe-Post` header on marketing (claim-outreach) email | Low (body-embedded unsubscribe link already satisfies CAN-SPAM's legal minimum; this is a deliverability best-practice gap, not a violation) | Engineering | None today | Add the RFC 8058 header once the ESP-specific support/value is confirmed | 10 |
| R-36 | **RESOLVED 2026-08-08.** The app-wide `--muted` design token (128 files) and a legal-page variant (8 files) both failed WCAG 2.2 AA color contrast (3.87-4.44:1 against real page backgrounds, need 4.5:1) | Was Medium (systemic, affecting most pages a visitor sees) | N/A — fixed | Both tokens darkened (`#6b6459`, `#5c6a63`), live-verified via a before/after axe-core scan across 9 pages | N/A — resolved | 11 |
| R-37 | **RESOLVED 2026-08-08.** Inline links in body text (auth pages, legal pages) relied on color alone to signal they were clickable (WCAG 1.4.1) | Was Low-Medium | N/A — fixed | Underline added across 8 files, live-verified | N/A — resolved | 11 |
| R-38 | **RESOLVED 2026-08-08.** Marketplace's 6 search filter controls (4 `<select>`, 2 `<input>`) had no programmatic label association -- the 4 selects had zero accessible name (WCAG 4.1.2, critical impact) -- screen-reader users could not tell what the search filters were for at all | Was Medium (a real usability blocker for screen-reader users on a core discovery feature) | N/A — fixed | `id`/`htmlFor` pairs added to all 6 controls, live-verified via re-scan | N/A — resolved | 11 |
| R-39 | No skip-to-main-content link or `<main>` landmark exists anywhere in the app; keyboard users must tab through the full navigation on every single page load | Medium (real friction for keyboard-only users, not a total blocker) | Engineering | None today | Needs a per-page-family approach since there is no shared layout shell across the app's 100+ page components | 11 (T33) |
| R-40 | Every `input/select/textarea:focus` rule (30 occurrences, same pattern) removes the default outline and replaces it with a border-color-only change -- a real but minimal focus indicator relative to WCAG 2.2's stronger focus-appearance guidance | Low | Engineering | None today | Strengthen with an added box-shadow/thicker border once a design pass can verify the look across all 30 sites | 11 (T32) |
| R-41 | **RESOLVED 2026-08-09.** `registerOrganization()` and `addOrganization()` (`server/src/db.ts`) stamped `organizations.platform_fee_rate` from the flat `TIERS` table instead of the role-aware `planCatalog.ts` lookup Section 05 (T25) had already fixed for the subscription-cancellation path -- client/installer/sponsor orgs (0% fee at every catalog tier) could get baked in with the generic 5%/2.5%/1% rate at creation time | Was Medium (billing-data-integrity defect; not yet reachable by real money since T7/Stripe keys remain unset) | N/A — fixed | Both call sites now use `planTierFor(role, tier)?.platformFeeRate ?? 0`, matching the already-correct pattern; numerically live-verified against the compiled plan catalog across every role x tier combination; idempotent backfill (`db/schema-fix-org-fee-rates.sql`) added for any rows already created wrong -- still needs to be RUN by the operator against staging/production (not run from this session; no database was reachable) | N/A — code fix resolved; backfill run is an operator action, see operator-actions.md | 12 |
| R-42 | **RESOLVED 2026-08-09.** The cookie-consent banner (`CookieBanner.tsx`) only ever showed once, on first visit; nothing in the app could reopen it once a choice was stored, despite `Cookies.tsx` telling visitors "use the cookie banner" to change their choice at any time -- a real gap between disclosed and actual behavior | Was Low-Medium (consent-withdrawal expectation, not a data-collection failure -- the collection itself was already correctly consent-gated) | N/A — fixed | Added `openCookiePreferences()` (dispatches an event the already-mounted banner listens for) and a "Manage cookie preferences" button on `Cookies.tsx`; live-verified end to end in a real browser | N/A — resolved | 13 |
| R-43 | **RESOLVED 2026-08-09.** `GET /api/healthz` returned an unconditional `{ok:true}` regardless of database reachability -- the app's single real dependency and its own most likely outage mode -- so deploy/uptime tooling that trusted this endpoint could report a fully broken app as healthy indefinitely | Was Medium (an outage this endpoint was specifically relied on to catch, per T4's own deploy-verification criteria, could go undetected) | N/A — fixed | Added a `select 1` check with a 1.5s cap; live-verified against a real, disposable Postgres instance in both states (200/db:true up, 503/db:false down, returned promptly) | N/A — resolved | 14 |
| R-44 | **RESOLVED 2026-08-09.** `incident-response-plan.md`'s Detection section stated "no error-monitoring or SIEM-style alerting integrated," which was true when written but false since structured logging + optional webhook alerting shipped 2026-08-03 -- a document meant to be relied on under pressure was underselling current capability | Was Low (documentation accuracy, not a technical control gap) | N/A — fixed | Rewrote the section to state current capability accurately, keeping the two genuinely-still-open items (429s not logged/alerted, no `audit_logs` anomaly scanning) clearly flagged as open | N/A — resolved | 14 |
| R-45 | No documented recovery procedure existed for total application-host loss (distinct from database-only restore, which Section 06 already covers); the deploy steps and the restore steps existed as two separate documents never combined into one sequence | Was Low-Medium (a real planning gap, though not yet a live incident) | N/A — fixed (as a documented plan) | New `compliance/policies/disaster-recovery-runbook.md`; explicitly marked DRAFT and not-yet-exercised end to end -- exercising the full sequence on a genuinely fresh host remains a future tabletop/staging exercise, and its off-host-storage/secrets preconditions remain unconfirmed operator facts | Operator: confirm off-host backup storage + off-host secrets recovery, then run a real dry-run exercise | 14 |
| R-46 | **RESOLVED 2026-08-09.** Malformed `:id`-shaped path params (SQL-injection-style payloads and simple typos alike) produced a raw 500 with a generic body across an unknown but likely large number of routes -- live-confirmed on 2 independent public routes -- because Postgres's own type-cast rejection (the query is parameterized, never string-concatenated, so this was never an injection risk) fell through to the generic error branch instead of a clean 4xx | Was Low-Medium (robustness/API-quality gap, not a security bypass; the malformed value never reached SQL syntax) | N/A — fixed | One central fix in the Express error handler recognizing Postgres error code `22P02`; live re-verified on both original routes plus a benign malformed ID and a real not-found case (confirmed unaffected) | N/A — resolved | 15 |
| R-47 | **RESOLVED 2026-08-09.** `mobile/PrivacyInfo.xcprivacy` (the Apple privacy manifest) did not declare Phone Number as a collected data type, despite `users.phone` being a real, populated column -- the file's own header requires the App Store Connect nutrition label to match it exactly | Was Medium (a mismatched privacy label risks App Review rejection/resubmission) | N/A — fixed | Added the Phone Number declaration matching the existing pattern; verified the file is well-formed XML (the strongest check available without macOS/Xcode) | N/A — resolved | 16 |
| R-48 | The compliance docs' own proposed mitigation for a borderline Apple IAP classification ("gate paid flows behind the web app when running in the native shell") has zero supporting implementation -- no code anywhere detects the app is running inside the native Capacitor shell | Low today (classification decision not yet made; becomes real risk only if App Review requires gating and none exists at submission time) | Product/legal, then engineering | None today | Make the IAP classification decision (case-by-case per paid flow, per the existing runbook), then build a small native-platform-detection helper if gating is actually needed | 16 |
