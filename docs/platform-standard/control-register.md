# Control Register

One row per control across all ALFY2 pack sections. Bootstrapped at Section
01; populated as each later section executes. Per pack Rule 2, this
register does not duplicate `AI_PROJECT_OS/12_TASK_QUEUE.md` (this project's
existing task/control tracker) — rows here cite it as evidence where a
control was already implemented and tracked there.

| Control ID | Section | Requirement | Priority | Applicability | Status | Evidence | Risk | Remediation | Validation | Owner/Action | Last Checked |
|---|---|---|---|---|---|---|---|---|---|---|---|
| S01-01 | 01 | Architecture/infrastructure inventory exists and is current | P1 | Required | PASS | `docs/platform-standard/architecture-map.md` | Low | N/A | Manually verified against `package.json`, `db/apply-all.sql`, `.github/workflows/ci.yml`, `server/src` env-var grep, 2026-08-08 | N/A | 2026-08-08 |
| S01-02 | 01 | Regulatory applicability matrix populated with evidence, not assumption | P0 | Required | PASS | `docs/platform-standard/applicability-register.md` §D | Low | N/A | Cross-checked against `AI_PROJECT_OS/52_COMPLIANCE.md`, `AI_PROJECT_OS/12_TASK_QUEUE.md` T7/T8, schema grep for health/education/biometric/background-check fields (none found) | Owner/Counsel per row (see applicability-register.md) | 2026-08-08 |
| S01-03 | 01 | Age gate at registration (COPPA hygiene) | P2 | Conditional | FAIL | `db/apply-all.sql` grep: no `date_of_birth`/`dob`/`age_confirmed` field found | Low (product is not child-directed, but zero technical barrier exists if a minor signs up) | Add an age-affirmation field/checkbox at registration | Not yet implemented | Owner | 2026-08-08 |
| S01-04 | 01 | Legal-acceptance versioning machinery exists | P1 | Required | PASS | `terms_acceptance` + `consent_records` tables, `db/apply-all.sql` lines ~62, ~4183 | Low | N/A | Confirmed schema exists; end-to-end functional test deferred to Section 02 | N/A | 2026-08-08 |
| S01-05 | 01 | Audit-trail table exists and is actively written | P1 | Required | PASS | `audit_logs` table + `server/src/lib/audit.ts`, wired into admin, MFA, payments, support, platform-revenue routes (confirmed via grep, not just schema presence) | Low | Retention period for `audit_logs` not yet centrally defined | Confirmed active call sites via grep, 2026-08-08 | N/A (retention policy is a Section 02/06 follow-up) | 2026-08-08 |
| S02-01 | 02 | Legal documents exist, are routed, and versioned acceptance is tracked | P1 | Required | PASS | `src/pages/{Terms,Privacy,PaymentPolicy,MarketplaceConduct,NonCircumvention,Cookies}.tsx`, routed in `App.tsx`; `terms_acceptance` table (agreement_version, policy_version, accepted_at) | Low | N/A | Confirmed routes exist and resolve; schema confirmed | N/A | 2026-08-08 |
| S02-02 | 02 | Consent/versioning architecture exists and functions | P1 | Required | PASS | `consent_records` table; `server/src/db/compliancePrivacy.ts` (`recordConsent`, `myConsents`); `routes/compliance-privacy.ts` `POST/GET /consent` | Low | N/A | Live-tested: non-admin submit/read own consent state works, 2026-08-08 | N/A | 2026-08-08 |
| S02-03 | 02 | Privacy-request (access/export/correction/deletion) architecture exists, is reachable, and works | P0 | Required | **PASS (was PARTIAL — fixed this session)** | `privacy_requests` table; `server/src/db/compliancePrivacy.ts`; `routes/compliance-privacy.ts`; `src/pages/ComplianceCenter.tsx` now also mounted at `/account/privacy` (`src/App.tsx`), linked from `src/pages/profile/ProfileEditor.tsx`'s Account tab | Was Medium (built but undiscoverable — no nav link, admin-sounding URL only, Privacy Policy didn't mention it); now Low | Fixed: added `/account/privacy` route, ProfileEditor link, Privacy.tsx copy update | Live-tested in a real browser with a non-admin account: link renders on Account tab, navigates correctly, page shows only non-admin-appropriate sections (submit request + consent, not retention policies); backend 403s a non-admin on the admin-only retention write, 2026-08-08 | N/A — resolved | 2026-08-08 |
| S02-04 | 02 | Data retention policy declared and enforced per data category | P1 | Required | PARTIAL | `data_retention_policies` table + tooling (`compliancePrivacy.ts`, `ComplianceCenter.tsx` admin panel) exists and works; `docs/platform-standard/data-retention-matrix.md` proposes actual content | Medium — mechanism works but 0 policies are declared today, and nothing enforces `data_retention_policies` automatically anywhere in the codebase even once declared (it is a declaration surface, not an enforcement job, by explicit schema comment) | Owner/counsel to confirm proposed periods in `data-retention-matrix.md`, then declare them; build enforcement jobs where warranted (see T14, T15) | Confirmed 0 rows via direct query, 2026-08-08 | Owner/counsel | 2026-08-08 |
| S02-05 | 02 | Tracking/cookie inventory matches consent mechanism and disclosure | P1 | Required | PASS | `src/components/CookieBanner.tsx` (localStorage `divini_consent_v1`, `consentGranted()`); its only real caller is `src/lib/fingerprint.ts` (gated behind `if (!consentGranted()) return;`); Section 01 confirmed no third-party analytics/ad SDK exists to also gate | Low | N/A | Traced the one real caller of `consentGranted()` to confirm the banner actually gates something, not decorative; cross-checked against Section 01's "no analytics SDK" finding, 2026-08-08 | N/A | 2026-08-08 |
| S02-06 | 02 | `visitor_signals` collection matches its disclosure and has a bounded retention | P2 | Required | FAIL | `server/src/routes/signals.ts` + `server/src/db/signals.ts`: collection matches `Privacy.tsx`'s disclosure in *nature*, but the disclosure states no retention period and the code confirms none is enforced (unbounded growth) | Low severity (not sensitive data, but genuinely unbounded) | Decide and implement a retention/purge job (T15) | Read both the route and db module directly to confirm no purge exists, 2026-08-08 | Owner (period), then implementation | 2026-08-08 |
| S02-07 | 02 | Legal-document set covers DMCA/copyright, AI-use disclosure, and an Accessibility Statement | P1 (DMCA, AI disclosure), P2 (Accessibility Statement) | Required (DMCA — UGC exists; AI disclosure — AI features exist), Conditional (Accessibility Statement — content depends on Section 11) | FAIL | Full-text search of `Terms.tsx`, `Privacy.tsx`, `MarketplaceConduct.tsx` and the page list under `src/pages/`: none of the three exist as standalone pages or sections | Medium (DMCA safe-harbor protection and AI transparency are both real, low-cost-to-add gaps; accessibility statement is lower urgency since it's a documentation artifact, not a control) | Add three DRAFT pages pending counsel review, per the existing `compliance/policies/` DRAFT pattern (T16, T17, T18) | Full-text grep across all three existing legal pages, 2026-08-08 | Owner/counsel | 2026-08-08 |
| S03-01 | 03 | No secrets committed to the repository, current tree or history | P0 | Required | PASS | Live regex scan for Stripe-live-key/AWS-key/PEM-private-key patterns across the current tree AND full git history (`git log --all -p`, all reachable commits) — zero matches; confirmed no `.env.local` or non-example `.env*` was ever added | Low | N/A | Actually executed, not assumed — see `docs/platform-standard/section-03-repo-supply-chain.md` | N/A | 2026-08-08 |
| S03-02 | 03 | CI installs with locked dependencies | P1 | Required | **PASS (was FAIL — fixed this session)** | `.github/workflows/ci.yml`: `npm install` → `npm ci` (root + server) | Was Low-Medium (CI could pass on a dependency resolution that differs from what a clean install elsewhere would produce) | Fixed | Verified `npm ci --dry-run` succeeds cleanly for both before committing the change | N/A — resolved | 2026-08-08 |
| S03-03 | 03 | CI actually builds the app, not just typechecks | P1 | Required | **PASS (was FAIL — fixed this session)** | `.github/workflows/ci.yml`: added `npm run build --prefix server` and `npm run build` (SPA) | Was Medium (a typecheck pass doesn't catch every build-time failure — e.g. Vite-specific issues) | Fixed | Ran the exact new CI sequence locally end-to-end before committing (typecheck → test → build server → build SPA → server audit), all passed | N/A — resolved | 2026-08-08 |
| S03-04 | 03 | Automated dependency-vulnerability alerting configured | P1 | Required | **PASS (was FAIL — fixed this session)** | `.github/dependabot.yml` added (root npm, server npm, GitHub Actions, weekly) | Was Medium (vulnerable dependencies could go unnoticed indefinitely) | Fixed | N/A — config addition, no local test applicable | N/A — resolved | 2026-08-08 |
| S03-05 | 03 | Dependency vulnerability scan gate in CI | P1 | Required | **PARTIAL (was FAIL — partially fixed this session)** | `server/` gated (0 vulnerabilities, blocking `npm audit --omit=dev` step added and verified passing); root/SPA intentionally not yet gated | Server: Low (now gated). Root: Medium — 11 findings all in Capacitor mobile-build-only devDependencies, not live production surface, but unresolved (see S03-06) | Root gate deferred until T19 (mobile-build-tooling audit fix) is resolved with real Xcode/Android verification | Ran `npm audit --omit=dev --prefix server` locally, 0 vulnerabilities, matches new CI step | Owner (T19) | 2026-08-08 |
| S03-06 | 03 | No unresolved dependency vulnerabilities | P2 | Required | FAIL | Root: 11 `npm audit` findings (3 moderate, 7 high, 1 critical), all traced via `npm ls xcode`/`npm ls tar` to `@capacitor/assets`/`@capacitor/cli`/`xcode` (mobile-build-only devDependencies). Server: 0. | Low real-world severity (confirmed not part of the deployed runtime — `deploy.sh` never runs `npm install`; mobile builds happen on a separate Mac per T9), but a real supply-chain hygiene gap | `npm audit fix --force` needed, requires mobile-build verification (T19) | Traced full dependency chains, applied the safe non-forcing fix already available | Owner, needs Mac/Xcode access | 2026-08-08 |
| S03-07 | 03 | License inventory contains no copyleft dependencies | P2 | Required | PASS | Full `license-checker` scan: all permissive (MIT/ISC/Apache-2.0/BSD/BlueOak/Unlicense/0BSD/CC0/CC-BY), zero GPL/AGPL/LGPL | Low | N/A | Actually run, not assumed | N/A | 2026-08-08 |
| S03-08 | 03 | Repository governance: CODEOWNERS, branch protection, tagged releases | P2 | Required | **PARTIAL (was FAIL/UNKNOWN — mostly fixed this session)** | `CODEOWNERS` added; `v0.1.0` tagged locally (push rejected, 403 -- not yet published, see operator-actions.md); redundant `pnpm-lock.yaml` removed and `build:server`/`build:all` converted to npm. Branch-protection status still UNKNOWN — a GitHub-UI setting this environment cannot check or set. | Was Low-Medium; now Low (branch protection is the one remaining unknown) | T21 (remainder: operator confirms/sets branch protection in GitHub settings) | `npm run build:all` verified working end to end after the pnpm→npm conversion | Owner (branch protection only) | 2026-08-08 |
| S03-09 | 03 | Database-restore has an identity/confirmation guard | P1 | Required | PASS | `server/src/scripts/restore-db.ts`'s `confirm()` requires explicit `--yes`, never auto-confirms | Low | Minor P2 enhancement: print the resolved target DB host/name before the confirmation prompt, not just require `--yes` blindly | Read the guard function directly, confirmed it throws without `--yes` | N/A (enhancement only) | 2026-08-08 |
| S03-10 | 03 | Linter installed, configured, and passing in CI | P2 | Required | **PASS (was FAIL — fixed this session)** | `eslint.config.js` (ESLint 10 flat config, typescript-eslint + react-hooks + react-refresh); `npm run lint`; CI gate added | Was Medium (no static analysis at all); now Low | Fixed — see the T20 changelog entry in `AI_PROJECT_OS/12_TASK_QUEUE.md` for the full list of real findings (1 Rules-of-Hooks bug, 1 unstable-key anti-pattern, 2 impure-render/regex fixes) vs. tuned-out noise (2 rules disabled with documented reasoning after sampling) | `npm run lint` exit code confirmed 0; ran against the full `src/` + `server/src/` tree | N/A — resolved | 2026-08-08 |
| S03-11 | 03 | Written secrets-rotation runbook | P2 | Required | **PASS (was PARTIAL — fixed this session)** | `compliance/policies/secrets-rotation-runbook.md` — full secret inventory with per-secret rotation impact/procedure | Low | N/A | N/A — documentation | N/A — resolved (still DRAFT status pending a named owner, per the folder's own convention) | 2026-08-08 |
| S04-01 | 04 | OAuth/social login applicability | N/A | N/A | N/A | Grepped `server/src/auth.ts` in full — no OAuth provider code exists; `src/lib/oidc.ts` (client) confirmed genuinely unimported anywhere in `src/` | N/A | N/A | Confirmed via grep, not assumed | N/A | 2026-08-08 |
| S04-02 | 04 | Valid login / invalid password / unverified-account handling | P0 | Required | PASS | Live-tested against a real registered account: valid login succeeds; wrong password gets a generic 401 regardless of verification state (verified BOTH directions, so verification status is never leaked to someone who hasn't proven they know the password); correct-password-but-unverified gets an explicit 403 | Low | N/A | 3 live HTTP round trips against a running server, 2026-08-08 | N/A | 2026-08-08 |
| S04-03 | 04 | Password reset: expiry + single-use (replay resistance) | P0 | Required | PASS | Forced a real token's `reset_expires` into the past via direct DB update, confirmed rejection; separately, used a valid token once (succeeded) then replayed it (rejected) | Low | N/A | 2 live end-to-end reset flows, 2026-08-08 | N/A | 2026-08-08 |
| S04-04 | 04 | Email-verification token: single-use | P1 | Required | PASS | Verified with the real token (succeeded), then replayed the identical token (rejected as invalid/expired) | Low | N/A | Live test, 2026-08-08 | N/A | 2026-08-08 |
| S04-05 | 04 | MFA (TOTP + backup codes) full round trip, including anti-replay of the login-time challenge token | P0 | Required | PASS | Computed real RFC 6238 codes from a live-enrolled secret (no authenticator app needed): enroll wrong/right code, login challenge wrong/right code, challenge token rejected when used as a Bearer session token, backup code accepted once and rejected on reuse | Low | N/A | Full live round trip via a purpose-built Python TOTP generator matching `lib/totp.ts`'s exact parameters (HMAC-SHA1/6-digit/30s), 2026-08-08 | N/A | 2026-08-08 |
| S04-06 | 04 | Session revocation on password reset | P0 | Required | PASS | Confirmed a session cookie issued BEFORE a password reset is dead immediately after (401 on `/me`) | Low | N/A | Live test, 2026-08-08 | N/A | 2026-08-08 |
| S04-07 | 04 | Standalone "sign out other sessions" action | P2 | Required | **PASS (was PARTIAL — fixed this session)** | `POST /auth/sign-out-other-sessions` added, reusing the already-verified `invalidateSessions()`; ProfileEditor "Sessions" section added | Was Low-Medium; now Low | Fixed | Live-tested with two independent device logins: calling device stays signed in (freshly re-issued), the other is immediately dead; frontend confirmed rendering in a real browser | N/A — resolved | 2026-08-08 |
| S04-08 | 04 | Rate limiting on repeated login failures | P1 | Required | PASS | 25 rapid wrong-password attempts: first ~17 got 401, then every subsequent attempt got 429 with a `retry_after_seconds` | Low | N/A | Live test against the running server, 2026-08-08 | N/A | 2026-08-08 |
| S04-09 | 04 | Forged client-side role/admin flag cannot grant authorization | P0 | Required | PASS | Logged in as a confirmed non-admin, sent `{"isAdmin":true,"role":"admin"}` in a request body to an admin-only write endpoint (403) and hit an admin-only route directly with the same session (403) | Low | N/A | Live adversarial test, 2026-08-08 | N/A | 2026-08-08 |
| S04-10 | 04 | Session cookie security attributes | P1 | Required | PASS | `httpOnly: true`, `secure: IS_PROD`, `sameSite: "lax"` — confirmed by direct code read of `setSessionCookie()` | Low | N/A | Code read | N/A | 2026-08-08 |
| S05-01 | 05 | No Postgres Row-Level Security in use (architectural fact, not a defect by itself) | P1 | Informational | PASS (documented) | `grep -ni "row level security\|create policy" db/apply-all.sql` — zero matches; all authorization is application-layer via `db.getActor()` | Medium (no DB-level defense-in-depth; a route that forgets to scope by `actor.org.id` has nothing else stopping it) | None required now; flagged as a standing architectural risk for all future org-scoped tables/routes | Confirmed via grep + read of `getActor()`, 2026-08-08 | N/A | 2026-08-08 |
| S05-02 | 05 | Cross-tenant read/write rejected on org-scoped resources | P0 | Required | PASS | Live test: two independently-registered orgs, Org B attempted read/write/status-change/vendor-attach on Org A's events, bids, quotes, invoices — 403/404 on all 11 attempts | Low | N/A | Live adversarial HTTP test against a running server with real registered accounts, `section-05-authorization.md` | N/A | 2026-08-08 |
| S05-03 | 05 | Forged org-membership (org-switch) rejected | P0 | Required | PASS | Live test: `POST /orgs/switch` with another org's id while authenticated as a non-member → 403 `not a member of that organization` | Low | N/A | Live test, 2026-08-08 | N/A | 2026-08-08 |
| S05-04 | 05 | Signed document / deck downloads are party-scoped (no IDOR) | P0 | Required | PASS | Code audit: `signatures.ts` `/:id/pdf` checks `isParty` before streaming; `profile-decks-programs.ts` deck download is org-scoped in the DB lookup itself (404 on mismatch, never a stream) | Low | N/A | Code read, both files in full, 2026-08-08 | N/A | 2026-08-08 |
| S05-05 | 05 | Admin impersonation / "view as" | N/A | N/A | N/A | Searched `server/src` for `impersonat\|viewAs\|actAs` — feature does not exist | N/A | N/A | Confirmed via grep, not assumed absent | N/A | 2026-08-08 |
| S05-06 | 05 | No route trusts a client-supplied organization_id/role for privilege escalation | P0 | Required | PASS | Grepped every route file for client-supplied `organization_id`/`user_id`/`role`; the only 2 hits (`bids.ts` invite, `events.ts` vendor-attach) are ownership-checked "invite another org" business logic, not self-privilege writes | Low | N/A | Full route-file grep + read, 2026-08-08 | N/A | 2026-08-08 |
| S05-07 | 05 | Cancelled subscription cannot retain premium (Pro/Plus) access | P1 | Required | PASS | Traced `applySubscriptionUpdate()`: cancellation sets `tier = 'free_partner'` immediately in the same transaction; `isTopTier()`/`checkLimit()` read `org.tier` fresh per request, never cached | Low | N/A | Code trace of the full Stripe webhook -> DB -> entitlements path, 2026-08-08 | N/A | 2026-08-08 |
| S05-08 | 05 | Privacy Policy technical claims match actual controls | P1 | Required | **PASS (was inaccurate — fixed this session)** | `src/pages/Privacy.tsx` claimed "database row-level security"; reworded to describe the real application-layer scoping mechanism | Was Low-Medium (misleading but not a security hole); now Low | Fixed | Diff review, 2026-08-08 | N/A — resolved | 2026-08-08 |
| S06-01 | 06 | Every table has a primary key | P0 | Required | PASS | Live query against `pg_class`/`pg_constraint` — zero tables without a PK, of 170 | Low | N/A | Command output, 2026-08-08 | N/A | 2026-08-08 |
| S06-02 | 06 | Core hot-path FKs have deliberate, sensible ON DELETE behavior | P1 | Required | PASS | `pg_constraint` query across `events`/`bids`/`quotes`/`invoices`/`event_vendors`/`organization_memberships` — 331 FKs, sensible CASCADE/SET NULL/RESTRICT mix; single NO ACTION FK confirmed inert (soft-delete pattern never triggers it) | Low | N/A | Command output + code trace, 2026-08-08 | N/A | 2026-08-08 |
| S06-03 | 06 | Tenant-key (`organization_id`/`user_id`) columns are indexed | P2 | Required | **PASS (was FAIL — fixed this session)** | 12 tables missing `organization_id` index, 2 missing `user_id` index, found via live `information_schema` query | Was Medium (scale risk, not correctness); now Low | Fixed | `db/schema-org-tenant-indexes.sql` applied live; re-query confirmed zero remaining gaps, 2026-08-08 | N/A — resolved | 2026-08-08 |
| S06-04 | 06 | Migration file is idempotent, additive-only, no silent destructive drops | P0 | Required | PASS | `db/apply-all.sql`: 170/170 `CREATE TABLE IF NOT EXISTS`, 191/191 `ALTER TABLE ADD COLUMN IF NOT EXISTS`, zero unguarded `DROP` | Low | N/A | Full-file grep, 2026-08-08 | N/A | 2026-08-08 |
| S06-05 | 06 | Deletion/anonymization does not corrupt referential integrity or transaction ledgers | P0 | Required | PASS | `deleteAccount()` soft-deletes/anonymizes the `users` row in a transaction rather than hard-deleting it; row persists so nothing referencing `users(id)` can dangle; organization deletion has no code path at all (N/A, not a gap) | Low | N/A | Code trace, 2026-08-08 | N/A | 2026-08-08 |
| S06-06 | 06 | Backup mechanism produces a real, restorable backup (not assumed) | P1 | Required | **PASS (previously unverified — tested this session)** | Real `pg_dump`/gzip/upload run against the live dev DB, then a real restore into a disposable scratch database, with exact row-count and table-count verification | Was unverified (built but never restore-tested); now Low | Fixed (verified) | Live end-to-end backup+restore exercise, `compliance/policies/backup-and-restore-runbook.md`, 2026-08-08 | N/A — resolved | 2026-08-08 |
| S06-07 | 06 | Payment webhook idempotency (no double-recording on Stripe retry) | P0 | Required | PASS | `payments.reference` has a real partial unique index (`uq_payments_reference`) backing `on conflict (reference) do nothing`; same pattern confirmed on `payout_excluded_transactions` | Low | N/A | `pg_indexes` query + code read, 2026-08-08 | N/A | 2026-08-08 |
| S06-08 | 06 | Platform credit redemption cannot be double-spent under concurrent requests | P0 | Required | **PASS (was FAIL — real race found and fixed live)** | `redeemCredit()` read balance then inserted the debit with no lock between them; 10 concurrent `POST /credits/redeem` requests against a $10 balance could over-redeem before the fix | Was High (real money-adjacent double-spend, live reachable route); now Low | Fixed | Live-reproduced against a real test account, fixed with a transaction + `pg_advisory_xact_lock`, re-tested with the same 10-concurrent-request scenario: exactly 1 succeeded, final balance exactly $0.00, 2026-08-08 | N/A — resolved | 2026-08-08 |
| S06-09 | 06 | Entitlement usage-limit checks (seats/events/inventory/packages/warehouses) are race-free | P2 | Required | FAIL (documented) | Same check-then-insert shape as S06-08 but for plan-limit counts, not money; traced concretely in `seats.ts` | Low-Medium (soft plan-limit overrun only, not monetary or cross-tenant) | Apply the same advisory-lock pattern proven in `credits.ts` to all 5 route files | Not yet implemented | Engineering (T26) | 2026-08-08 |
| S07-01 | 07 | Perimeter security headers (CSP/HSTS/X-Frame-Options/etc.) | P1 | Required | PASS | Live `curl -D -` against the running server on every check; `server/src/lib/securityHeaders.ts` | Low | N/A | Live header inspection, 2026-08-08 | N/A | 2026-08-08 |
| S07-02 | 07 | CORS fails closed in production | P0 | Required | PASS | `server/src/app.ts` — empty allowlist denies cross-origin in prod, permissive only outside prod | Low | N/A | Code read, 2026-08-08 | N/A | 2026-08-08 |
| S07-03 | 07 | No framework/version info disclosure via response headers | P2 | Required | **PASS (was FAIL — fixed this session)** | `X-Powered-By: Express` was present on every response | Was Low; now none | Fixed | `app.disable("x-powered-by")`, confirmed live before/after, 2026-08-08 | N/A — resolved | 2026-08-08 |
| S07-04 | 07 | No SQL injection via string-interpolated queries | P0 | Required | PASS | Full-tree grep for `query(\`...${...}\`)` — zero hits; one dynamic ORDER BY confirmed to select from a fixed allowlist, not raw input | Low | N/A | Command output, 2026-08-08 | N/A | 2026-08-08 |
| S07-05 | 07 | SSRF guard on all user-supplied-URL server-side fetches | P0 | Required | PASS | `lib/safe-fetch.ts` blocks private/loopback/link-local/metadata IP ranges and re-validates redirects; confirmed as the actual mechanism used at the one real user-URL fetch site (`discovery-search.ts`) | Low | N/A | Code read + call-site trace, 2026-08-08 | N/A | 2026-08-08 |
| S07-06 | 07 | Open redirect protection | P1 | Required | PASS | `routes/email-track.ts`'s click-redirect validates against `PUBLIC_APP_URL`/`ALLOWED_ORIGINS`; SPA swept for the `searchParams`-into-`navigate()` pattern, zero matches | Low | N/A | Code read + grep, 2026-08-08 | N/A | 2026-08-08 |
| S07-07 | 07 | Mass-assignment protection | P1 | Required | PASS | Every route destructures named fields; the 3 `{...req.body}` spread sites traced through to their db-layer, which only reads its own explicitly-typed fields (server-resolved actor fields, e.g. `organization_id`, are never read from the spread input) | Low | N/A | Code trace, 2026-08-08 | N/A | 2026-08-08 |
| S07-08 | 07 | File upload: magic-byte / MIME / extension cross-validation | P0 | Required | PASS | Live adversarial test: magic-byte-mismatch PDF (400), disguised PE-executable-as-PDF (400) | Low | N/A | Live multipart upload test against the running server, 2026-08-08 | N/A | 2026-08-08 |
| S07-09 | 07 | File upload: filename/path traversal sanitation | P0 | Required | PASS | Live test: `filename=../../../../etc/passwd.pdf` landed only at the sanitized, org-scoped storage path, confirmed on disk directly | Low | N/A | Live upload test + `find` on the storage root, 2026-08-08 | N/A | 2026-08-08 |
| S07-10 | 07 | Uploaded file metadata stripping (EXIF/PDF author fields) | P2 | Conditional | FAIL (documented) | No stripping step exists; images/PDFs stored with original embedded metadata intact | Low (current file mix is mostly business documents, not personal photos) | Add a metadata-stripping step once a library is chosen | Not yet implemented | Engineering (T28) | 2026-08-08 |
| S07-11 | 07 | Stale "no MFA" claims in security-module comments | P2 | Required | **PASS (was inaccurate — fixed this session)** | `securityHeaders.ts` and `rateLimit.ts` both claimed no MFA exists, false since Section 04 | Was Low (doc accuracy); now none | Fixed | Diff review, 2026-08-08 | N/A — resolved | 2026-08-08 |
| S08-01 | 08 | Prompt-injection defense: untrusted content fenced and never obeyed as instructions | P0 | Required | PASS | `lib/promptSafety.ts`'s `wrapUntrustedContent()` + `UNTRUSTED_CONTENT_SYSTEM_SUFFIX`, confirmed used consistently at all 3 real LLM call sites | Low | N/A | Code read, all 3 call sites, 2026-08-08 | N/A | 2026-08-08 |
| S08-02 | 08 | AI output never directly writes a live record; requires human accept/edit | P0 | Required | PASS | `ai_profile_suggestions` table: every extraction lands as `status='ai_suggested_pending_verification'`, promoted to the live profile only via an explicit `POST /onboarding/suggestions/:id` action | Low | N/A | Schema + code read, 2026-08-08 | N/A | 2026-08-08 |
| S08-03 | 08 | Model cannot set its own role/owner/price/payout/access level | P0 | Required | PASS | Extracted fields are limited to descriptive profile content only; no role/ownership/payout field is ever populated from model output | Low | N/A | Code read, 2026-08-08 | N/A | 2026-08-08 |
| S08-04 | 08 | LLM is never a hard dependency (graceful degradation on provider failure) | P1 | Required | PASS | Live-tested: `POST /profile/extract` against an unreachable Ollama backend returned `available:false` cleanly, no crash/hang/500 | Low | N/A | Live HTTP test against the running server, 2026-08-08 | N/A | 2026-08-08 |
| S08-05 | 08 | Secrets never injected into LLM prompt context | P0 | Required | PASS | Grepped all 3 prompt-construction files for session/password/token/secret identifiers — zero matches | Low | N/A | Command output, 2026-08-08 | N/A | 2026-08-08 |
| S08-06 | 08 | `ai_run_audit`-equivalent trail for AI extraction calls | P2 | Required | **PASS (was FAIL — fixed this session)** | Neither extraction route logged to `audit_logs`, inconsistent with every other privileged action in the codebase | Was Low (observability gap, not exploitable); now none | Fixed | `logAction("ai.extract_profile", ...)` added to both routes; live-verified a real audit_logs row was written with provider/model/outcome (never the extracted content) | N/A — resolved | 2026-08-08 |
| S08-07 | 08 | Structured accuracy / eval harness for AI extraction quality | P2 | Conditional | FAIL (documented) | No golden test set or automated accuracy measurement exists | Low (human-reviewed, non-hard-dependency feature at current volume) | Build an eval harness once extraction volume justifies it | Not yet implemented | Engineering, deferred | 2026-08-08 |
| S09-01 | 09 | PCI scope minimized (Checkout-only, no server-side cardholder data) | P0 | Required | PASS | Full-tree grep for PAN/CVC handling — zero real matches; all payment collection via Stripe Checkout Sessions | Low | N/A | Command output + architecture trace, 2026-08-08 | N/A | 2026-08-08 |
| S09-02 | 09 | No client-selected connected-account/payout-destination authority | P0 | Required | PASS | Grepped for client-supplied Stripe account/destination id in request bodies — zero matches; every payout destination resolved via `getPayoutAccount(actor.org.id, ...)` | Low | N/A | Command output + code trace, 2026-08-08 | N/A | 2026-08-08 |
| S09-03 | 09 | Webhook signature verification (HMAC + replay-window bound) | P0 | Required | PASS | `lib/processors.ts`'s `verifyStripeEvent()`; live-tested: forged signature and missing signature header both correctly rejected (400) with `STRIPE_WEBHOOK_SECRET` unset | Low | N/A | Live HTTP test against the running server, 2026-08-08 | N/A | 2026-08-08 |
| S09-04 | 09 | Event-level webhook idempotency ledger (`webhook_events`) | P1 | Required | **PASS (was FAIL — fixed this session)** | No ledger existed; idempotency was payment-row-level only, leaving `account.updated`/`customer.subscription.*`/v2 capability events with zero dedup protection | Was Medium; now Low | Fixed | `db/schema-webhook-events.sql`, `db/webhookEvents.ts`, wired into both Stripe and PayPal handlers; live-verified the dedup insert logic directly against the database (first insert succeeds, duplicate no-ops) and confirmed rejected forgeries never populate the ledger | N/A — resolved | 2026-08-08 |
| S09-05 | 09 | Out-of-order webhook delivery tolerance | P2 | Conditional | FAIL (documented) | `applySubscriptionUpdate()` applies whatever status arrives with no timestamp comparison against a prior event | Low (narrow, self-healing in the common case) | Build a last-processed-event-timestamp guard if this becomes a real issue at volume | Not yet implemented | Engineering, deferred | 2026-08-08 |
| S09-06 | 09 | Refund/dispute-response capability | P1 (conditional on T7) | Conditional | FAIL (documented) | Full-tree grep for a refund API call or `charge.dispute.*` handler — zero matches; only bookkeeping fields reference "refunds" | Medium once real money is live; none today (T7 still blocks real money) | Build an admin-triggered refund action + dispute webhook handler before T7 unblocks | Not yet implemented | Engineering (T30), before T7 | 2026-08-08 |
| S09-07 | 09 | Single canonical promotion/value engine (no parallel coupon systems) | P2 | Required | PASS | Only one value-granting mechanism exists (`platform_credits`); the one other candidate found (`founding-member.ts`) is a status/badge feature, confirmed it writes no pricing/fee-rate fields | Low | N/A | Code read, 2026-08-08 | N/A | 2026-08-08 |
| S09-08 | 09 | Live processor test-mode end-to-end flows (Checkout completion, real signed webhook, proration, dunning, Connect payout) | P0 | Required | **BLOCKED** | `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`STRIPE_PUBLISHABLE_KEY` all unset in this environment | N/A until credentials provided | Operator: set real Stripe TEST-mode keys in this environment, then re-run this validation | Cannot be validated without credentials | Operator (same gate as T7) | 2026-08-08 |
| S10-01 | 10 | Email authentication: SPF, DKIM, DMARC all correctly published and aligned for the real production domain | P0 | Required | PASS | Live DNS lookups (two independent resolvers) against `divinipartners.com`: DKIM at `resend._domainkey`, DMARC at `_dmarc` (`p=quarantine`), SPF correctly on the `send.` return-path subdomain (`v=spf1 include:amazonses.com ~all`), return-path MX to `feedback-smtp.us-east-1.amazonses.com` | Low | N/A | Command output (live DNS), 2026-08-08 | N/A | 2026-08-08 |
| S10-02 | 10 | Transactional vs. marketing email separation | P1 | Required | PASS | Only the Claim Engine's outreach channel carries opt-in/opt-out machinery (`decideSend()`, suppression checks, 6-send cap); transactional mail correctly carries none of it | Low | N/A | Code read, 2026-08-08 | N/A | 2026-08-08 |
| S10-03 | 10 | CAN-SPAM: sender ID, non-deceptive framing, functioning opt-out, physical address | P1 | Required | PASS | `complianceFooter()` in `lib/claim-emails.ts`, read in full | Low | N/A | Code read, 2026-08-08 | N/A | 2026-08-08 |
| S10-04 | 10 | Cross-channel bounce/complaint suppression (not just claim-outreach-specific) | P1 | Required | **PASS (was FAIL — fixed this session)** | `sendEmail()`, the one shared transport for all email, had no suppression check at all; nothing ever auto-populated a bounce reason anywhere in the tree | Was Medium (deliverability + compliance risk); now Low | Fixed | `communication_suppressions` table + `sendEmail()` filter + Resend webhook; live-verified fail-closed signature rejection, DB-level suppression matching, and the actual `sendEmail()` per-recipient filtering behavior (mixed-recipient test) | N/A — resolved | 2026-08-08 |
| S10-05 | 10 | `List-Unsubscribe`/`List-Unsubscribe-Post` header on marketing email | P2 | Conditional | FAIL (documented) | No such header is set on any outreach send | Low (body-embedded unsubscribe link already satisfies the legal minimum; this is a deliverability best-practice gap) | Add the header once the ESP-specific value/support is confirmed | Not yet implemented | Engineering (T31), deferred | 2026-08-08 |
| S10-06 | 10 | SMS / push notification compliance (TCPA, permission state, token rotation) | N/A | N/A | N/A | Zero SMS-sending or push-notification code exists anywhere in the codebase; `planCatalog.ts`'s "SMS Package" is an unbuilt future add-on concept only. Confirmed with the user this is an intentional v2 deferral, not a gap to close now. | N/A | N/A | Full-tree grep, 2026-08-08 | N/A | 2026-08-08 |
| S11-01 | 11 | WCAG 2.2 AA color contrast (normal text, 4.5:1) | P1 | Required | **PASS (was FAIL — fixed this session)** | Live axe-core scan (9 pages, before/after): `--muted` token (#7d776c, 128 files) and a legal-page variant (#6b7a72, 8 files) both failed AA against real page backgrounds | Was Medium (systemic, affecting most pages); now Low | Fixed | Both tokens darkened (#6b6459, #5c6a63), verified via a second live axe-core pass across all 9 tested pages showing 0 remaining real violations | N/A — resolved | 2026-08-08 |
| S11-02 | 11 | Links embedded in body text are distinguishable without relying on color alone (WCAG 1.4.1) | P1 | Required | **PASS (was FAIL — fixed this session)** | axe `link-in-text-block` on Login/Register/Terms — inline auth/legal links used color only, no underline | Was Low-Medium; now Low | Fixed | `text-decoration:underline` added across 8 files, live-verified via re-scan | N/A — resolved | 2026-08-08 |
| S11-03 | 11 | Form controls have a programmatically associated accessible name (WCAG 4.1.2) | P0 | Required | **PASS (was FAIL — fixed this session)** | axe `select-name` (critical impact): Marketplace's 6 search filters had visually-adjacent but unassociated `<label>`s, 4 `<select>` elements had zero accessible name | Was Medium (screen-reader users could not use the marketplace search filters at all); now Low | Fixed | `id`/`htmlFor` pairs added to all 6 controls, live-verified via re-scan showing the finding gone | N/A — resolved | 2026-08-08 |
| S11-04 | 11 | Marketing-hero color contrast (white text on dark gradient hero sections) | N/A | N/A | **PASS — confirmed false positive** | axe flagged white hero text as low-contrast against the light page background; verified via a real browser screenshot that the text renders on a dark emerald gradient (a z-index-layered sibling axe cannot see) with genuinely high contrast | Low | N/A | Playwright screenshot, `pricing_hero.png`, 2026-08-08 | N/A | 2026-08-08 |
| S11-05 | 11 | Skip-to-main-content link / `<main>` landmark | P1 | Required | FAIL (documented) | No skip link or `<main>` landmark exists anywhere; confirmed via grep | Medium (keyboard users must tab through full nav every page) | Needs a per-page-family approach given the app has no shared layout shell | Not yet implemented | Engineering (T33) | 2026-08-08 |
| S11-06 | 11 | Focus indicator strength (WCAG 2.2 focus-appearance guidance) | P2 | Conditional | PARTIAL (documented) | All 30 `input/select/textarea:focus` rules use a border-color-only change; a real but minimal indicator | Low | Strengthen with an added box-shadow/thicker border for full 2.2 robustness | Not yet implemented | Engineering (T32), deferred | 2026-08-08 |
| S11-07 | 11 | Alt text on all images | P0 | Required | PASS | Full-tree grep for `<img` without `alt=` — zero matches | Low | N/A | Command output, 2026-08-08 | N/A | 2026-08-08 |
| S11-08 | 11 | Onboarding checklist completion tracked from real backend state | P1 | Required | PASS | `Onboarding.tsx`'s `stepsCompleted`/`currentStep` persisted via the existing `PUT /onboarding` endpoint, read back from `state.draft.completion_status` | Low | N/A | Code read, 2026-08-08 | N/A | 2026-08-08 |
| S12-01 | 12 | Organization `platform_fee_rate` correctly role-aware at every write site (registration, multi-org add, subscription lifecycle) | P1 | Required | **PASS (was FAIL — fixed this session)** | `registerOrganization()` and `addOrganization()` in `server/src/db.ts` stamped `platform_fee_rate` from the flat `TIERS` table instead of the role-aware `planTierFor()` lookup Section 05 (T25) had already fixed for the subscription-cancellation path; `client`/`installer`/`sponsor` (whose catalog fee is 0 at every tier) could get baked in with the generic 5%/2.5%/1% rate | Was Medium (billing-data-integrity, not yet reachable by real money since T7 is deferred); now Low | Fixed | Both call sites now use `planTierFor(role, tier)?.platformFeeRate ?? 0`; numerically live-verified against the compiled plan catalog across every role x tier combination (see section-12 report); idempotent backfill script added for any rows already created wrong | Operator: run `db/schema-fix-org-fee-rates.sql` (or `db/apply-all.sql`) against staging/production | 2026-08-09 |
| S12-02 | 12 | Multi-org switcher never allows switching into an org the user is not a member of | P0 | Required | PASS | `switchActiveOrganization()` checks `organization_memberships` before updating the session's active org | Low | N/A | Code read, 2026-08-09 | N/A | 2026-08-09 |
| S12-03 | 12 | Profile routes (onboarding, AI extraction, theme, publish, transfer-owner) are organization-scoped and the transfer-owner authorization model matches the platform's actual single-owner-per-org membership design | P0 | Required | PASS | Full read of `profiles.ts` (418 lines); every authed route resolves org via `getActor`; `transfer-owner`'s "any member of the active org can transfer" is correct because `organization_memberships` is one-human-many-orgs, not many-humans-one-org (team seats are billing-only, never create a second login-capable member) | Low | N/A | Code read + cross-check against `schema-org-membership.sql` and `team_seats` usage, 2026-08-09 | N/A | 2026-08-09 |
| S12-04 | 12 | Admin routes (platform admin + admin-manage listings/agreements) gated and audit-logged | P0 | Required | PASS | `admin.ts`/`admin-manage.ts` both router-level `requireAdmin`; parameterized SQL throughout; every mutating route calls `logAction` with before/after state | Low | N/A | Code read, 2026-08-09 | N/A | 2026-08-09 |
| S12-05 | 12 | Packages (products/services) CRUD is org-scoped and entitlement-limited | P1 | Required | PASS | Full read of `packages.ts`; every route resolves org via `getActor`, `POST /` checks `checkLimit` before creation | Low | N/A | Code read, 2026-08-09 | N/A | 2026-08-09 |
| S12-06 | 12 | Calendar mutation routes are org-scoped; the public `.ics` feed and public-availability endpoint leak no private detail; feed tokens are unguessable | P0 | Required | PASS | `updateCalendarEvent`/`deleteCalendarEvent` scope by `organization_id`; `publicAvailability` returns only merged busy windows (no title/description/kind); feed tokens are `crypto.randomBytes(24)` | Low | N/A | Code read, 2026-08-09 | N/A | 2026-08-09 |
| S12-07 | 12 | Compliance documents (COI/W-9/e-sign — the platform's actual "documents" feature) are org-scoped, approval is admin-gated, and callers cannot create a document under another org | P0 | Required | PASS | `listDocuments` scopes to `organization_id or owner_id` unless admin; `setDocApproval` requires admin; `createDocument` stamps `owner_id`/`organization_id` from the actor only | Low | N/A | Code read, 2026-08-09 | N/A | 2026-08-09 |
| S12-08 | 12 | Video subsection (interactive venue-tour builder, not video-conferencing) is auth-gated | N/A | N/A | PASS | `venue-twin.ts` router-level `requireUser`; true video-conferencing confirmed N/A in Section 01 (no such integration exists) | Low | N/A | Code read, 2026-08-09 | N/A | 2026-08-09 |
| S13-01 | 13 | No third-party analytics/tracking provider exists (confirms Section 01's applicability note) | N/A | N/A | PASS | Full-tree grep for GA/gtag/GTM/Segment/Mixpanel/Amplitude/Hotjar/FullStory/Meta Pixel/Clarity/PostHog — zero matches | Low | N/A | Command output, 2026-08-09 | N/A | 2026-08-09 |
| S13-02 | 13 | First-party visitor-signal collection (device fingerprint, IP, UTM) is consent-gated at every call site before any network request fires | P1 | Required | PASS | `reportSignal()` in `src/lib/fingerprint.ts` checks `consentGranted()` before sending; confirmed both call sites (`Landing.tsx`, `GetStarted.tsx`) go through it, no bypass found | Low | N/A | Code read, 2026-08-09 | N/A | 2026-08-09 |
| S13-03 | 13 | Cookie consent can be withdrawn/changed after the initial choice, matching what the Cookie Policy tells visitors | P2 | Required | **PASS (was FAIL — fixed this session)** | The consent banner only ever showed once, on first visit; nothing in the app could reopen it, despite `Cookies.tsx` telling visitors to "use the cookie banner" to change their choice at any time | Was Low-Medium (a real gap between disclosed and actual behavior) | Fixed | `openCookiePreferences()` reopen mechanism + a "Manage cookie preferences" button on `Cookies.tsx`; live-verified end to end in a real browser (initial banner, accept, reopen via button, change to reject, storage updates) | N/A — resolved | 2026-08-09 |
| S13-04 | 13 | Automated per-entity scoring (Divini Score) is transparent to the org it belongs to and does not constitute an unlawful automated decision about an individual | N/A | N/A | PASS | Deterministic, formula-based score of a business entity (not a consumer), org-resolved and actor-validated before any read, components visible to the owning org via `GET` | Low | N/A | Code read, 2026-08-09 | N/A | 2026-08-09 |

## Notes

- This register will grow substantially starting with Section 02. Sections
  already substantively covered by prior work in this repository (MFA,
  session revocation, structured logging, automated backups, CSRF, bot
  guard, prompt-injection defense, Stripe Accounts v2) will largely convert
  existing `AI_PROJECT_OS/` documentation into rows here rather than
  re-auditing from scratch, per pack Rule 2.
